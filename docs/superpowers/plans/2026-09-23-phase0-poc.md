# 0단계 PoC 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 스펙 §14의 PoC-1~10을 시뮬레이터 우선으로 구현·측정해 각 기능의 실현 가능성 판정표를 만들고, 판정에 따라 스펙을 갱신한다.

**Architecture:** iOS 쪽은 xcodegen으로 생성한 PoC 앱 하나(앱 + Share Extension + 로컬 Swift 패키지 `AssistantCore`)에 모든 기기 PoC를 넣는다. 서버 쪽은 호스팅 Supabase 무료 프로젝트 하나에 마이그레이션·Edge Function·deno 테스트를 둔다. PoC 코드는 `poc/` 아래에 두고 제품 코드로 복사하지 않는다. 판정 결과는 `docs/superpowers/poc/results.md`에 누적한다.

**Tech Stack:** Swift 6 / SwiftUI / App Intents / Foundation Models / EventKit / UserNotifications / SQLite(GRDB 없이 `SQLite3` C API), xcodegen, XCTest, Supabase CLI 2.75 (Postgres 17, pgvector, pg_cron, pg_net), Deno 2.7 Edge Functions, OpenAI SDK `npm:openai@7`(Responses API) — 분류·추출 `gpt-6-luna`, 임베딩 `text-embedding-3-small`(`dimensions: 512`). 키는 `OPENAI_API_KEY` 하나.

**Spec:** `docs/superpowers/specs/2026-09-22-assistant-design.md`

**진행 현황 (2026-09-24):** Task 1~7 완료(커밋은 각 태스크 머리의 "구현 결과"). Opus 재검증 A·B 반영은 `0d2a293`. **Task 1~7의 코드 블록은 원안이다.** 실제 동작의 원본은 저장소 코드와 스펙 §6·§10이며, 원안과 달라진 점은 각 태스크의 "구현 결과"에 적었다. 판정 상태는 `docs/superpowers/poc/results.md`.

## Global Constraints

- 기기 대상: iPhone 15 Pro 이상, iOS 26+. 시뮬레이터는 `iPhone 17 Pro` (iOS 26.3 런타임). 실기기 단계는 각 태스크에 "실기기" 표시.
- 서명: Team `6626BYCJG4`, Automatic. 번들 ID `com.picpal.assistant.poc`, App Group `group.com.picpal.assistant`.
- 빌드·테스트 전에 `vm_stat | grep -E 'free|compressor'`로 메모리 확인. Docker는 쓰지 않는다(호스팅 Supabase).
- 시뮬레이터는 세션마다 전용 UDID를 하나 만든다. 다른 세션 기기를 만지지 않는다.
- LLM: 추출·분류 `gpt-6-luna`, Responses API `text.format` json_schema(`strict: true`), `reasoning: { effort: "none" }`, **모든 호출 `store: false`**(스펙 §12 통제 3). 모델 ID는 스펙 §3 검증 표 그대로 쓰고 추측으로 바꾸지 않는다. 0단계에는 채팅 답변 생성이 없다(검색만 평가).
- 비밀값은 `.env`(gitignore)와 Supabase secrets에만. 코드·커밋에 넣지 않는다.
- 판정 기준은 스펙 §14 표를 그대로 쓴다. 통과/실패/대안 채택을 `results.md`에 기록한다.
- PoC 코드는 `poc/` 아래. 폐기 대상이며 제품 코드에 복사하지 않는다.

## 파일 구조

```text
poc/
  ios/
    project.yml                      # xcodegen
    App/
      AssistantPoCApp.swift          # @main, 알림 델리게이트 등록
      ContentView.swift              # PoC별 버튼·결과 표시 (큐·poc.log)
      CaptureIntent.swift            # PoC-1/2 App Intent
      Shortcuts.swift                # AppShortcutsProvider
      NotificationActions.swift      # PoC-5 카테고리·액션 핸들러
      Uploader.swift                 # PoC-9 background URLSession
      Info.plist
      App.entitlements
    ShareExtension/
      ShareViewController.swift      # PoC-8 로컬 영속화
      Info.plist
      ShareExtension.entitlements
    Packages/AssistantCore/
      Package.swift
      Sources/AssistantCore/
        RuleFilter.swift             # OTP·카드·계좌 규칙
        CaptureQueue.swift           # App Group SQLite 큐 (WAL, claim lease, 백오프)
        CapturePipeline.swift        # 규칙 → FM 결과 라우팅 → 큐 (Task 4·5)
        PoCLog.swift                 # poc.log (O_APPEND), BFULog (보호 등급 none, 길이만)
        OCR.swift                    # Vision OCR (Task 7)
        Executions.swift             # PoC-5 멱등 기록
        FMClassifier.swift           # PoC-3 Foundation Models
        AppGroup.swift               # 컨테이너 경로 상수
      Tests/AssistantCoreTests/
        RuleFilterTests.swift
        CaptureQueueTests.swift
        ExecutionsTests.swift
        FMClassifierTests.swift
        CaptureIntentTests.swift     # CapturePipeline·폴백 라우팅
        PoCLogTests.swift
        Fixtures/notifications.json  # 200건 분류 샘플
    UITests/
      SimRemeasureUITests.swift      # 시뮬레이터 재실측: 알림 권한·배너 액션·단축어 앱·사진 공유 시트
    scripts/
      sim.sh                         # 전용 시뮬레이터 부팅·빌드·설치·테스트
      push.sh                        # simctl push 페이로드 전송
      payloads/add_event.apns
  server/
    supabase/
      config.toml
      migrations/
        0001_jobs.sql                # Task 8
        0002_cron.sql                # Task 8
        0003_items_keys.sql          # Task 8: items(content_enc)·user_keys·audit_log
        0004_hybrid_search.sql       # Task 11
        0005_gmail.sql               # Task 10
      functions/
        _shared/job.ts
        _shared/crypto.ts            # 봉투 암호화 (MASTER_KEY → 사용자 DEK → AES-256-GCM)
        _shared/rules.ts             # 서버 규칙 필터 (기기 RuleFilter와 같은 규칙 + 프로모션)
        _shared/openai.ts            # OpenAI 클라이언트 (Task 11)
        _shared/embeddings.ts        # embed() — text-embedding-3-small 512차원 (Task 11)
        _shared/extract.ts           # extractEvent() — gpt-6-luna Structured Outputs (Task 12)
        _shared/apns.ts
        _shared/gmail.ts
        _shared/gmail-jobs.ts        # gmail-sync·gmail-fetch·gmail-watch 잡 핸들러
        ingest/handler.ts            # JWT → 규칙 → 암호화 → insert_item
        ingest/index.ts
        worker/index.ts              # PoC-10
        worker/process.ts            # 복호화 → 추출
        apns-send/index.ts           # PoC-4
        gmail-connect/index.ts       # PoC-6
        gmail-webhook/index.ts       # PoC-6
        vision-extract/index.ts      # PoC-8 서버 부분
      tests/
        jobs.test.ts
        crypto.test.ts
        rules.test.ts
        ingest.test.ts
        apns.test.ts
        gmail.test.ts
        search.test.ts
    scripts/
      seed-items.ts                  # 합성 항목 암호화 적재 (PoC-10)
    eval/
      corpus.jsonl                   # 500건
      questions.jsonl                # 50문항
      run-search-eval.ts
      images/                        # 청첩장 5종 (git 제외)
    .env.example
docs/superpowers/poc/
  results.md                         # 판정표 (누적)
  poc-1-notification-trigger.md      # 실기기 절차·기록
  poc-2-message-trigger.md
  poc-3-fm-classifier.md
  poc-5-notification-eventkit.md
  poc-8-9-share-upload.md
```

---

### Task 1: iOS PoC 프로젝트 골격

> **구현 결과 (완료):** 커밋 `a2b722b`, 리뷰 반영 `0d2a293`. 원안과 차이: `Package.swift` swift-tools 6.2(Xcode 26.3, 언어 모드는 Swift 6 그대로), 테스트 타깃 `GENERATE_INFOPLIST_FILE: YES`, `sim.sh`에 `uitest`·`log` 추가와 `.sim-udid` 없이 `gen` 동작, UI 테스트 타깃 `AssistantPoCUITests`(스킴 `AssistantPoCUI`). 아래 블록은 이 차이를 반영해 고쳤다.

**Files:**
- Create: `poc/ios/project.yml`, `poc/ios/App/AssistantPoCApp.swift`, `poc/ios/App/ContentView.swift`, `poc/ios/App/Info.plist`, `poc/ios/App/App.entitlements`, `poc/ios/Packages/AssistantCore/Package.swift`, `poc/ios/Packages/AssistantCore/Sources/AssistantCore/AppGroup.swift`, `poc/ios/Packages/AssistantCore/Tests/AssistantCoreTests/AppGroupTests.swift`, `poc/ios/scripts/sim.sh`, `.gitignore`

**Interfaces:**
- Produces: `AppGroup.id: String = "group.com.picpal.assistant"`, `AppGroup.containerURL() throws -> URL`, 스크립트 `scripts/sim.sh {boot|gen|build|test [id]|uitest [id]|log [n]|install|launch}`.

- [x] **Step 1: 메모리 확인 후 전용 시뮬레이터 생성**

```bash
vm_stat | grep -E 'free|compressor'
xcrun simctl create "assistant-poc" "iPhone 17 Pro" "com.apple.CoreSimulator.SimRuntime.iOS-26-3"
# 출력된 UDID를 poc/ios/.sim-udid 에 저장 (gitignore)
```

- [x] **Step 2: project.yml 작성**

```yaml
name: AssistantPoC
options:
  bundleIdPrefix: com.picpal.assistant
  deploymentTarget:
    iOS: "26.0"
  xcodeVersion: "26.3"
settings:
  base:
    DEVELOPMENT_TEAM: 6626BYCJG4
    CODE_SIGN_STYLE: Automatic
    SWIFT_VERSION: "6.0"
packages:
  AssistantCore:
    path: Packages/AssistantCore
targets:
  AssistantPoC:
    type: application
    platform: iOS
    sources: [App]
    dependencies:
      - package: AssistantCore
      - target: ShareExtension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.picpal.assistant.poc
        CODE_SIGN_ENTITLEMENTS: App/App.entitlements
        INFOPLIST_FILE: App/Info.plist
  ShareExtension:
    type: app-extension
    platform: iOS
    sources: [ShareExtension]
    dependencies:
      - package: AssistantCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.picpal.assistant.poc.share
        CODE_SIGN_ENTITLEMENTS: ShareExtension/ShareExtension.entitlements
        INFOPLIST_FILE: ShareExtension/Info.plist
  AssistantCoreTests:
    type: bundle.unit-test
    platform: iOS
    sources: [Packages/AssistantCore/Tests/AssistantCoreTests]
    dependencies:
      - package: AssistantCore
      - target: AssistantPoC
    settings:
      base:
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/AssistantPoC.app/AssistantPoC
        BUNDLE_LOADER: $(TEST_HOST)
        GENERATE_INFOPLIST_FILE: YES          # 테스트 번들에 Info.plist 가 없으면 빌드 실패
  # 시뮬레이터 재실측용 (Task 4·6·7 재실측에서 추가): 알림 권한 대화상자·배너 액션·단축어 앱·사진 공유 시트를 springboard 로 조작
  AssistantPoCUITests:
    type: bundle.ui-testing
    platform: iOS
    sources: [UITests]
    dependencies:
      - target: AssistantPoC
    settings:
      base:
        TEST_TARGET_NAME: AssistantPoC
        GENERATE_INFOPLIST_FILE: YES
schemes:
  AssistantPoC:
    build:
      targets: { AssistantPoC: all, ShareExtension: all, AssistantCoreTests: [test] }
    test:
      targets: [AssistantCoreTests]
  AssistantPoCUI:
    build:
      targets: { AssistantPoC: all, ShareExtension: all, AssistantPoCUITests: [test] }
    test:
      targets: [AssistantPoCUITests]
```

테스트 번들을 앱 호스트로 돌리는 이유: App Group 컨테이너와 Foundation Models는 호스트 앱 프로세스가 필요하다.

- [x] **Step 3: 엔타이틀먼트·Info.plist**

`App/App.entitlements`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0"><dict>
  <key>com.apple.security.application-groups</key>
  <array><string>group.com.picpal.assistant</string></array>
  <key>aps-environment</key><string>development</string>
</dict></plist>
```

`App/Info.plist` 핵심 키:
```xml
<key>NSCalendarsFullAccessUsageDescription</key><string>PoC: 제안된 일정을 캘린더에 추가합니다.</string>
<key>NSRemindersFullAccessUsageDescription</key><string>PoC: 할 일을 미리알림에 추가합니다.</string>
<key>NSContactsUsageDescription</key><string>PoC: 연락처에 있는 발신자의 메시지를 제외합니다.</string>
<key>UIBackgroundModes</key><array><string>remote-notification</string></array>
```

- [x] **Step 4: AssistantCore 패키지와 AppGroup**

`Package.swift`:
```swift
// swift-tools-version: 6.2
import PackageDescription
let package = Package(
  name: "AssistantCore",
  platforms: [.iOS(.v26)],
  products: [.library(name: "AssistantCore", targets: ["AssistantCore"])],
  targets: [
    .target(name: "AssistantCore"),
  ]
)
```

`AppGroup.swift`:
```swift
import Foundation
public enum AppGroup {
  public static let id = "group.com.picpal.assistant"
  public enum Error: Swift.Error { case containerUnavailable }
  public static func containerURL() throws -> URL {
    guard let url = FileManager.default.containerURL(forSecurityApplicationGroupIdentifier: id) else {
      throw Error.containerUnavailable
    }
    return url
  }
}
```

- [x] **Step 5: 실패하는 테스트**

`AppGroupTests.swift`:
```swift
import XCTest
@testable import AssistantCore
final class AppGroupTests: XCTestCase {
  func testContainerExists() throws {
    let url = try AppGroup.containerURL()
    XCTAssertTrue(FileManager.default.fileExists(atPath: url.path))
  }
}
```

- [x] **Step 6: 앱 진입점과 최소 UI**

`AssistantPoCApp.swift`:
```swift
import SwiftUI
@main struct AssistantPoCApp: App {
  var body: some Scene { WindowGroup { ContentView() } }
}
```
`ContentView.swift`: `Text("Assistant PoC")` 하나.

- [x] **Step 7: sim.sh 스크립트**

```bash
#!/bin/bash
set -euo pipefail
cd "$(dirname "$0")/.."
UDID=$(cat .sim-udid 2>/dev/null || true)   # gen 은 기기 없이도 동작
DEST="platform=iOS Simulator,id=$UDID"
case "${1:-}" in
  boot)  xcrun simctl boot "$UDID" 2>/dev/null || true; open -a Simulator ;;
  gen)   xcodegen generate ;;
  build) xcodebuild -project AssistantPoC.xcodeproj -scheme AssistantPoC -destination "$DEST" -derivedDataPath build build 2>&1 | tail -20 ;;
  test)  xcodebuild -project AssistantPoC.xcodeproj -scheme AssistantPoC -destination "$DEST" -derivedDataPath build test ${2:+-only-testing:"$2"} 2>&1 | grep -E "Test Case|passed|failed|error:" ;;
  uitest) xcodebuild -project AssistantPoC.xcodeproj -scheme AssistantPoCUI -destination "$DEST" -derivedDataPath build test ${2:+-only-testing:"$2"} 2>&1 | grep -E "Test Case|passed|failed|error:|POC_UI" ;;
  log)   cat "$(xcrun simctl get_app_container "$UDID" com.picpal.assistant.poc group.com.picpal.assistant)/poc.log" | tail -${2:-40} ;;
  install) xcrun simctl install "$UDID" build/Build/Products/Debug-iphonesimulator/AssistantPoC.app ;;
  launch) xcrun simctl launch "$UDID" com.picpal.assistant.poc ;;
  *) echo "usage: sim.sh {boot|gen|build|test [id]|uitest [id]|log [n]|install|launch}"; exit 1 ;;
esac
```

- [x] **Step 8: 생성·빌드·테스트 실행**

```bash
chmod +x poc/ios/scripts/sim.sh
poc/ios/scripts/sim.sh gen && poc/ios/scripts/sim.sh boot && poc/ios/scripts/sim.sh test
```
Expected: `AppGroupTests.testContainerExists passed`. 실패하면 서명 팀·App Group 자동 생성 여부를 Xcode에서 확인한다(Automatic 서명이 App Group을 포털에 등록한다).

**새 테스트·소스 파일을 추가하면 `sim.sh gen`을 먼저 다시 돌린다.** `.xcodeproj`는 xcodegen 산출물(gitignore)이라 gen 없이는 새 파일이 타깃에 들어가지 않아 "테스트 없음"으로 조용히 통과한다. 이후 모든 태스크에 적용.

주의: 시뮬레이터는 App Group 프로비저닝을 강제하지 않아 이 테스트는 엔타이틀먼트가 없어도 통과한다. 서명·포털 등록은 실기기 설치 때 확인한다.

- [x] **Step 9: .gitignore와 커밋**

```bash
printf 'poc/ios/build/\npoc/ios/*.xcodeproj\npoc/ios/.sim-udid\n.env\npoc/server/eval/images/\n' >> .gitignore
git add poc/ios .gitignore && git commit -m "poc(ios): project skeleton with App Group and test host"
```

---

### Task 2: 규칙 필터 (AssistantCore.RuleFilter)

> **구현 결과 (완료):** 커밋 `05bd500`, 리뷰 반영 `0d2a293`. 아래 정규식은 원안이며 **흔한 입력을 놓쳐 교체됐다**(하이픈 계좌, 날짜 뒤 카드번호 평문 노출, "인증코드"·"login code" 누락, "Hotpot"·연도 숫자 오탐). 현재 규칙은 스펙 §6 "기기 규칙 필터" 표와 `RuleFilter.swift`가 원본: OTP 키워드 확장·영문 경계·키워드 ±30자 창·승인번호+결제 문맥 예외, 카드 형식 고정(날짜 접두 대응), 계좌 하이픈형 10~14·연속형 10~16자리·키워드 ±20자·키워드 확장. 회귀 테스트 25개가 `RuleFilterTests`에 추가됐다(총 32). 타입은 `Sendable`. 연락처 규칙은 단위 테스트로만 검증됐다(Task 4 Step 6).

**Files:**
- Create: `poc/ios/Packages/AssistantCore/Sources/AssistantCore/RuleFilter.swift`, `.../Tests/AssistantCoreTests/RuleFilterTests.swift`

**Interfaces:**
- Produces:
```swift
public enum RuleVerdict: Equatable { case discard(reason: String), pass(masked: String) }
public struct RuleFilter {
  public init(contactNames: Set<String> = [])
  public func apply(text: String, sender: String?) -> RuleVerdict
}
```

- [x] **Step 1: 실패하는 테스트**

```swift
import XCTest
@testable import AssistantCore
final class RuleFilterTests: XCTestCase {
  let f = RuleFilter(contactNames: ["김민수"])
  func testOTPDiscarded() {
    XCTAssertEqual(f.apply(text: "[Web발신] 인증번호 483920 을 입력하세요", sender: nil), .discard(reason: "otp"))
  }
  func testVerificationEnglishDiscarded() {
    XCTAssertEqual(f.apply(text: "Your verification code is 1234", sender: nil), .discard(reason: "otp"))
  }
  func testCardMasked() {
    // 4532015112830366 은 Luhn 통과
    XCTAssertEqual(f.apply(text: "카드 4532-0151-1283-0366 승인 32,000원", sender: nil),
                   .pass(masked: "카드 ****-****-****-0366 승인 32,000원"))
  }
  func testNonLuhnNumberKept() {
    XCTAssertEqual(f.apply(text: "송장번호 1234567890123", sender: nil), .pass(masked: "송장번호 1234567890123"))
  }
  func testAccountMasked() {
    XCTAssertEqual(f.apply(text: "국민은행 계좌 12345678901234 로 입금", sender: nil),
                   .pass(masked: "국민은행 계좌 **********1234 로 입금"))
  }
  func testContactSenderDiscarded() {
    XCTAssertEqual(f.apply(text: "토요일 2시에 보자", sender: "김민수"), .discard(reason: "contact"))
  }
  func testPlainNoticePasses() {
    XCTAssertEqual(f.apply(text: "내일 오후 3시 진료 예약입니다", sender: "서울병원"), .pass(masked: "내일 오후 3시 진료 예약입니다"))
  }
}
```

- [x] **Step 2: 실행해 실패 확인**

`poc/ios/scripts/sim.sh test AssistantCoreTests/RuleFilterTests` → "cannot find 'RuleFilter'"

- [x] **Step 3: 구현**

```swift
import Foundation

public enum RuleVerdict: Equatable { case discard(reason: String), pass(masked: String) }

public struct RuleFilter {
  private let contactNames: Set<String>
  public init(contactNames: Set<String> = []) { self.contactNames = contactNames }

  private static let otpKeyword = try! NSRegularExpression(pattern: #"(인증|승인|확인)\s*번호|verification|OTP"#, options: [.caseInsensitive])
  private static let otpDigits  = try! NSRegularExpression(pattern: #"(?<!\d)\d{4,8}(?!\d)"#)
  private static let cardLike   = try! NSRegularExpression(pattern: #"(?<!\d)(?:\d[ -]?){12,18}\d(?!\d)"#)
  private static let account    = try! NSRegularExpression(pattern: #"(은행|계좌).{0,20}?(?<!\d)(\d{10,14})(?!\d)"#)

  public func apply(text: String, sender: String?) -> RuleVerdict {
    if let s = sender, contactNames.contains(s) { return .discard(reason: "contact") }
    let ns = text as NSString
    let all = NSRange(location: 0, length: ns.length)
    if Self.otpKeyword.firstMatch(in: text, range: all) != nil, Self.otpDigits.firstMatch(in: text, range: all) != nil {
      return .discard(reason: "otp")
    }
    var out = text
    // 카드번호: Luhn 통과하는 13~19자리만 마스킹
    for m in Self.cardLike.matches(in: text, range: all).reversed() {
      let raw = ns.substring(with: m.range)
      let digits = raw.filter(\.isNumber)
      guard (13...19).contains(digits.count), Self.luhn(digits) else { continue }
      let last4 = String(digits.suffix(4))
      var masked = ""; var seen = 0
      for ch in raw {
        if ch.isNumber { seen += 1; masked.append(seen > digits.count - 4 ? ch : "*") } else { masked.append(ch) }
      }
      _ = last4
      out = (out as NSString).replacingCharacters(in: m.range, with: masked)
    }
    // 계좌번호
    let outNS = out as NSString
    for m in Self.account.matches(in: out, range: NSRange(location: 0, length: outNS.length)).reversed() {
      let r = m.range(at: 2)
      let num = outNS.substring(with: r)
      let masked = String(repeating: "*", count: num.count - 4) + num.suffix(4)
      out = (out as NSString).replacingCharacters(in: r, with: masked)
    }
    return .pass(masked: out)
  }

  static func luhn(_ digits: String) -> Bool {
    var sum = 0; var alt = false
    for ch in digits.reversed() {
      guard var d = ch.wholeNumberValue else { return false }
      if alt { d *= 2; if d > 9 { d -= 9 } }
      sum += d; alt.toggle()
    }
    return sum % 10 == 0
  }
}
```

- [x] **Step 4: 테스트 통과 확인**

`poc/ios/scripts/sim.sh test AssistantCoreTests/RuleFilterTests` → 7 passed

- [x] **Step 5: 커밋**

```bash
git add poc/ios/Packages && git commit -m "poc(core): rule filter for OTP, card, account, contact sender"
```

---

### Task 3: App Group SQLite 큐 (CaptureQueue)

> **구현 결과 (완료):** 커밋 `70563ca`, 리뷰 반영 `0d2a293`. 원안의 큐는 앱·확장 동시 쓰기에서 `SQLITE_BUSY`로 **200건 중 162건을 잃었다.** 현재: `busy_timeout` 3초 + `journal_mode=WAL` + `synchronous=NORMAL`, step 오류는 throw(빈 큐로 삼키지 않음), 디코딩 불가 행은 건너뜀, `ORDER BY created_at, rowid`, `next_attempt_at` 컬럼으로 지수 백오프(`markFailed(id:now:)`, 30초×2^n, 상한 1시간), `pending(limit:now:)`, 선점 조회 `claim(limit:now:)`(lease 600초, `UPDATE … RETURNING`). `CaptureItem.deviceFilter` 추가. 테스트 11개(동시 두 연결 200건, 백오프, claim 중복 없음, poison row 등). 스펙 §6 "큐"·"업로더".

**Files:**
- Create: `.../Sources/AssistantCore/CaptureQueue.swift`, `.../Tests/AssistantCoreTests/CaptureQueueTests.swift`

**Interfaces:**
- Produces:
```swift
public struct CaptureItem: Codable, Equatable {
  public var id: String            // UUID
  public var source: String        // "MESSAGES" | "NOTIFICATION" | "SHARE"
  public var appName: String?
  public var sender: String?
  public var title: String?
  public var text: String
  public var localFile: String?    // App Group 상대 경로 (이미지·PDF)
  public var ocrText: String?
  public var capturedAt: Date
  public var attempts: Int
}
public final class CaptureQueue {
  public init(url: URL) throws            // <container>/queue.sqlite
  public static func shared() throws -> CaptureQueue
  public func enqueue(_ item: CaptureItem) throws
  public func pending(limit: Int) throws -> [CaptureItem]
  public func markSent(id: String) throws
  public func markFailed(id: String) throws   // attempts += 1
}
```

- [x] **Step 1: 실패하는 테스트**

```swift
import XCTest
@testable import AssistantCore
final class CaptureQueueTests: XCTestCase {
  func makeQueue() throws -> CaptureQueue {
    let url = FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString + ".sqlite")
    return try CaptureQueue(url: url)
  }
  func item(_ text: String) -> CaptureItem {
    CaptureItem(id: UUID().uuidString, source: "NOTIFICATION", appName: "KakaoTalk", sender: nil, title: nil,
                text: text, localFile: nil, ocrText: nil, capturedAt: Date(), attempts: 0)
  }
  func testEnqueueAndPending() throws {
    let q = try makeQueue()
    try q.enqueue(item("a")); try q.enqueue(item("b"))
    XCTAssertEqual(try q.pending(limit: 10).map(\.text), ["a", "b"])
  }
  func testMarkSentRemoves() throws {
    let q = try makeQueue(); let i = item("a")
    try q.enqueue(i); try q.markSent(id: i.id)
    XCTAssertTrue(try q.pending(limit: 10).isEmpty)
  }
  func testMarkFailedIncrementsAttempts() throws {
    let q = try makeQueue(); let i = item("a")
    try q.enqueue(i); try q.markFailed(id: i.id)
    XCTAssertEqual(try q.pending(limit: 10).first?.attempts, 1)
  }
  func testSurvivesReopen() throws {
    let url = FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString + ".sqlite")
    try CaptureQueue(url: url).enqueue(item("persist"))
    XCTAssertEqual(try CaptureQueue(url: url).pending(limit: 1).first?.text, "persist")
  }
}
```

- [x] **Step 2: 실행해 실패 확인** → "cannot find 'CaptureQueue'"

- [x] **Step 3: 구현 (SQLite3 C API, 외부 의존 없음)**

```swift
import Foundation
import SQLite3

public struct CaptureItem: Codable, Equatable {
  public var id: String; public var source: String; public var appName: String?; public var sender: String?
  public var title: String?; public var text: String; public var localFile: String?; public var ocrText: String?
  public var capturedAt: Date; public var attempts: Int
  public init(id: String, source: String, appName: String?, sender: String?, title: String?, text: String,
              localFile: String?, ocrText: String?, capturedAt: Date, attempts: Int) {
    self.id = id; self.source = source; self.appName = appName; self.sender = sender; self.title = title
    self.text = text; self.localFile = localFile; self.ocrText = ocrText; self.capturedAt = capturedAt; self.attempts = attempts
  }
}

public final class CaptureQueue {
  public enum Error: Swift.Error { case sqlite(String) }
  private var db: OpaquePointer?
  private let enc = JSONEncoder(), dec = JSONDecoder()

  public init(url: URL) throws {
    guard sqlite3_open_v2(url.path, &db, SQLITE_OPEN_READWRITE | SQLITE_OPEN_CREATE | SQLITE_OPEN_FULLMUTEX, nil) == SQLITE_OK else {
      throw Error.sqlite(String(cString: sqlite3_errmsg(db)))
    }
    try exec("CREATE TABLE IF NOT EXISTS queue(id TEXT PRIMARY KEY, payload BLOB NOT NULL, attempts INTEGER NOT NULL DEFAULT 0, created_at REAL NOT NULL)")
    // 잠금 중에도 접근 가능해야 함: 첫 잠금 해제 후 보호 등급
    try? FileManager.default.setAttributes([.protectionKey: FileProtectionType.completeUntilFirstUserAuthentication], ofItemAtPath: url.path)
  }
  deinit { sqlite3_close(db) }

  public static func shared() throws -> CaptureQueue {
    try CaptureQueue(url: try AppGroup.containerURL().appendingPathComponent("queue.sqlite"))
  }

  public func enqueue(_ item: CaptureItem) throws {
    let data = try enc.encode(item)
    try run("INSERT OR IGNORE INTO queue(id,payload,attempts,created_at) VALUES(?,?,?,?)") { s in
      sqlite3_bind_text(s, 1, item.id, -1, Self.transient)
      data.withUnsafeBytes { sqlite3_bind_blob(s, 2, $0.baseAddress, Int32(data.count), Self.transient) }
      sqlite3_bind_int(s, 3, Int32(item.attempts))
      sqlite3_bind_double(s, 4, item.capturedAt.timeIntervalSince1970)
    }
  }
  public func pending(limit: Int) throws -> [CaptureItem] {
    var out: [CaptureItem] = []
    var s: OpaquePointer?
    guard sqlite3_prepare_v2(db, "SELECT payload, attempts FROM queue ORDER BY created_at LIMIT ?", -1, &s, nil) == SQLITE_OK else { throw Error.sqlite(msg) }
    defer { sqlite3_finalize(s) }
    sqlite3_bind_int(s, 1, Int32(limit))
    while sqlite3_step(s) == SQLITE_ROW {
      let bytes = sqlite3_column_blob(s, 0), len = sqlite3_column_bytes(s, 0)
      var item = try dec.decode(CaptureItem.self, from: Data(bytes: bytes!, count: Int(len)))
      item.attempts = Int(sqlite3_column_int(s, 1))
      out.append(item)
    }
    return out
  }
  public func markSent(id: String) throws {
    try run("DELETE FROM queue WHERE id=?") { sqlite3_bind_text($0, 1, id, -1, Self.transient) }
  }
  public func markFailed(id: String) throws {
    try run("UPDATE queue SET attempts=attempts+1 WHERE id=?") { sqlite3_bind_text($0, 1, id, -1, Self.transient) }
  }

  private static let transient = unsafeBitCast(-1, to: sqlite3_destructor_type.self)
  private var msg: String { String(cString: sqlite3_errmsg(db)) }
  private func exec(_ sql: String) throws { guard sqlite3_exec(db, sql, nil, nil, nil) == SQLITE_OK else { throw Error.sqlite(msg) } }
  private func run(_ sql: String, bind: (OpaquePointer) -> Void) throws {
    var s: OpaquePointer?
    guard sqlite3_prepare_v2(db, sql, -1, &s, nil) == SQLITE_OK, let st = s else { throw Error.sqlite(msg) }
    defer { sqlite3_finalize(st) }
    bind(st)
    guard sqlite3_step(st) == SQLITE_DONE else { throw Error.sqlite(msg) }
  }
}
```

- [x] **Step 4: 테스트 통과 확인** → 4 passed

- [x] **Step 5: 커밋** `git commit -m "poc(core): App Group SQLite capture queue"`

---

### Task 4: CaptureIntent + 단축어 노출 (PoC-1, PoC-2)

> **구현 결과 (완료):** 커밋 `964cfea`, 리뷰 반영 `0d2a293`. 원안과 차이: `text` 기본값 `""`(본문이 비어 와도 단축어가 값을 묻지 않음), 로그에 값 없이 필드 메타(`app=` `titleLen=` `textLen=` `sender=set|nil` `locked=`), 오류도 로그 후 throw, 규칙 폐기 사유(`discarded:otp`/`contact`) 유지, BFU 전용 로그(`BFULog`, 보호 등급 none, 길이만). Step 3(단축어 앱 수동 실행)은 XCUITest `testShortcutsAppRunsCaptureIntent`로 수행(`CaptureIntent queued:rules … textLen=0`). 판정 상태: PoC-1/2 **미검증**(실기기 필요). 연락처 로딩은 Step 6(완료).

**Files:**
- Create: `poc/ios/App/CaptureIntent.swift`, `poc/ios/App/Shortcuts.swift`, `poc/ios/Packages/AssistantCore/Tests/AssistantCoreTests/CaptureIntentTests.swift`, `docs/superpowers/poc/poc-1-notification-trigger.md`, `docs/superpowers/poc/poc-2-message-trigger.md`
- Modify: `poc/ios/App/ContentView.swift` (큐 내용 표시)

**Interfaces:**
- Consumes: `RuleFilter`, `CaptureQueue`
- Produces: `struct CaptureIntent: AppIntent` — 파라미터 `text: String`, `appName: String?`, `title: String?`, `sender: String?`, `source: String`. `supportedModes = .background`, `authenticationPolicy = .alwaysAllowed`. 결과 `IntentResult` 다이얼로그에 "queued"/"discarded:<reason>".

- [x] **Step 1: 인텐트 로직을 순수 함수로 분리하고 테스트**

`AssistantCore/CapturePipeline.swift` (AssistantCore에 추가):
```swift
import Foundation
public struct CapturePipeline {
  public let filter: RuleFilter
  public let queue: CaptureQueue
  public init(filter: RuleFilter, queue: CaptureQueue) { self.filter = filter; self.queue = queue }
  /// 반환: "queued" 또는 "discarded:<reason>"
  public func handle(source: String, appName: String?, title: String?, sender: String?, text: String) throws -> String {
    switch filter.apply(text: text, sender: sender) {
    case .discard(let r): return "discarded:\(r)"
    case .pass(let masked):
      try queue.enqueue(CaptureItem(id: UUID().uuidString, source: source, appName: appName, sender: sender, title: title,
                                    text: masked, localFile: nil, ocrText: nil, capturedAt: Date(), attempts: 0))
      return "queued"
    }
  }
}
```

`CaptureIntentTests.swift`:
```swift
import XCTest
@testable import AssistantCore
final class CapturePipelineTests: XCTestCase {
  func testNoticeQueued() throws {
    let q = try CaptureQueue(url: FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString))
    let p = CapturePipeline(filter: RuleFilter(), queue: q)
    XCTAssertEqual(try p.handle(source: "NOTIFICATION", appName: "KakaoTalk", title: "쿠팡", sender: nil, text: "주문이 접수되었습니다"), "queued")
    XCTAssertEqual(try q.pending(limit: 1).first?.appName, "KakaoTalk")
  }
  func testOTPNotQueued() throws {
    let q = try CaptureQueue(url: FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString))
    let p = CapturePipeline(filter: RuleFilter(), queue: q)
    XCTAssertEqual(try p.handle(source: "MESSAGES", appName: nil, title: nil, sender: "15880000", text: "인증번호 123456"), "discarded:otp")
    XCTAssertTrue(try q.pending(limit: 1).isEmpty)
  }
}
```
실행 → 실패 확인 → 위 구현 추가 → 통과.

- [x] **Step 2: App Intent**

`CaptureIntent.swift`:
```swift
import AppIntents
import UIKit
import AssistantCore

struct CaptureIntent: AppIntent {
  static let title: LocalizedStringResource = "비서에 저장"
  static let description = IntentDescription("알림·메시지 텍스트를 비서 큐에 저장합니다")
  static let supportedModes: IntentModes = .background
  static let authenticationPolicy: IntentAuthenticationPolicy = .alwaysAllowed

  @Parameter(title: "본문") var text: String
  @Parameter(title: "앱 이름") var appName: String?
  @Parameter(title: "제목") var title: String?
  @Parameter(title: "발신자") var sender: String?
  @Parameter(title: "출처", default: "NOTIFICATION") var source: String

  func perform() async throws -> some IntentResult & ProvidesDialog {
    let started = Date()
    let pipeline = CapturePipeline(filter: RuleFilter(), queue: try CaptureQueue.shared())
    let result = try pipeline.handle(source: source, appName: appName, title: title, sender: sender, text: text)
    let ms = Int(Date().timeIntervalSince(started) * 1000)
    PoCLog.append("CaptureIntent \(result) \(ms)ms locked=\(UIApplication.shared.isProtectedDataAvailable == false)")
    return .result(dialog: "\(result)")
  }
}
```

`PoCLog`는 AssistantCore에 추가: App Group의 `poc.log`에 한 줄씩 append하는 `public enum PoCLog { public static func append(_ line: String) }`. ContentView가 이 파일과 큐를 읽어 보여준다.

`Shortcuts.swift`:
```swift
import AppIntents
struct AssistantShortcuts: AppShortcutsProvider {
  static var appShortcuts: [AppShortcut] {
    AppShortcut(intent: CaptureIntent(), phrases: ["\(.applicationName)에 저장"], shortTitle: "비서에 저장", systemImageName: "tray.and.arrow.down")
  }
}
```

- [x] **Step 3: 시뮬레이터에서 인텐트 수동 실행 확인**

`sim.sh build && sim.sh install && sim.sh launch`. 시뮬레이터의 단축어 앱에서 새 단축어 → "비서에 저장" 액션 → 본문 입력 → 실행. 앱을 열어 큐에 1건 표시되는지 확인. 이것으로 인텐트 배선은 시뮬레이터에서 검증된다.

- [x] **Step 4: 실기기 절차 문서 작성 (PoC-1)**

`docs/superpowers/poc/poc-1-notification-trigger.md`:
```markdown
# PoC-1 Notification 트리거 (실기기)

## 준비
- iPhone에 AssistantPoC 설치 (Xcode → Run, 케이블). 설정 → 알림 → 카카오톡·Instagram 미리보기 "항상".
- 단축어 앱 → 자동화 → 새 자동화 → **알림** → 앱: 카카오톡, 필터 없음 → "즉시 실행" 선택 가능 여부 기록. 같은 방법으로 Instagram 알림 자동화도 만든다(PoC-2 메시지 자동화까지 합쳐 자동화 3개: 카톡 알림·인스타 알림·메시지).
- 동작: "비서에 저장" 액션 추가. 본문 파라미터에 단축어 입력을 연결할 수 있는 변수 목록을 스크린샷.

## 시나리오 (각각 poc.log 확인)
| # | 상황 | 기록할 것 |
|---|---|---|
| 1 | 잠금 해제 상태, 카톡 알림톡 수신 | 확인 배너 표시 여부, 인텐트에 온 text/title/appName 값 |
| 2 | 잠금 상태 수신 | 실행 여부, locked= 값 |
| 3 | 미리보기 "잠금 해제 시" 설정 후 잠금 상태 수신 | text 비어 있는지 |
| 4 | 5초 안에 알림 3건 (묶음) | 3건 모두 큐에 왔는지 |
| 5 | Instagram DM 알림 | title/text 형식 |
| 6 | 필터 "주문" 설정 후 "주문" 없는 알림 | 트리거 안 됨 확인 |
| 7 | (iOS 27 기기가 있다면) 1~2 반복 | 차이 |

## 판정
스펙 §14 PoC-1 기준으로 통과/실패, 대안 채택 여부를 results.md에 기록.
```

`poc-2-message-trigger.md`도 같은 형식으로: 자동화 → 메시지 → 발신자/문구 조건, "즉시 실행"+"잠금 시 실행 허용" 켜기, 시나리오 (잠금 해제 수신 / 잠금 수신 / 재부팅 후 첫 잠금 해제 전 수신 / OTP 문자 → discarded:otp 확인).

- [x] **Step 5: 커밋** `git commit -m "poc(ios): CaptureIntent, shortcuts provider, PoC-1/2 device procedures"`

- [x] **Step 6: 연락처 로딩 (추가, 실기기 세션 전에 수행)**

> **구현 결과:** AssistantCore `ContactNames`가 이름 집합을 App Group `contacts.json`에 캐시한다(이름만). 앱이 실행·포그라운드 복귀 때 `CNContactStore`로 갱신하고, 권한 요청은 앱의 "권한 요청 (알림·캘린더·연락처)" 버튼에서 한다. 권한 미허용이면 빈 집합을 캐시하고 `poc.log`에 `contacts status=<상태> names=0`을 남긴다. 인텐트는 캐시만 읽는다. 정규화는 원안(앞뒤 공백·"님")과 달리 **모든 공백 제거 + 끝의 호칭 "님"/"씨" 제거**이고, `sender`와 `title` 양쪽을 비교해 `discarded:contact`로 폐기한다. PoC-1/2 절차 문서에 연락처 시나리오를 추가했다. 아래 1~5는 원안이다.

재검증에서 발견된 범위 공백이다. 앱·인텐트가 `RuleFilter()`를 연락처 없이 만들어 스펙 §6 연락처 규칙이 동작하지 않는다.
1. `ContactNames` (AssistantCore): 앱이 포그라운드에서 `CNContactStore.requestAccess(for: .contacts)` 후 `enumerateContacts`로 표시 이름을 모아 App Group `contacts.json`에 저장(이름만, 보호 등급 completeUntilFirstUserAuthentication). 앱 실행·복귀 때 갱신.
2. `CaptureIntent`·`ContentView`가 `RuleFilter(contactNames: ContactNames.cached())`로 만든다. 인텐트는 연락처 DB를 직접 읽지 않는다(백그라운드 비용).
3. `RuleFilter.apply`가 `sender`와 `title`을 모두 비교하고, 앞뒤 공백과 호칭 "님"을 뗀 뒤 비교한다.
4. 테스트: `"김민수님"` title → `discarded:contact`, `" 김민수 "` sender → 폐기, 연락처에 없는 이름 → 통과. 새 테스트 파일이면 `sim.sh gen` 먼저.
5. PoC-1/2 절차에 "연락처에 저장한 두 번째 계정에서 보낸 카톡·문자 → `discarded:contact`" 시나리오를 추가.

---

### Task 5: Foundation Models 분류기 (PoC-3)

> **구현 결과 (완료, 수치 미측정):** 커밋 `b1f3248`, 리뷰 반영 `0d2a293`. 원안의 세션 재사용은 **틀렸다**: 세션은 transcript를 누적해 4,096토큰에서 `exceededContextWindowSize`를 던지고, actor 재진입 때문에 동시 요청(`concurrentRequests`)도 막지 못한다. 현재: 호출마다 새 `LanguageModelSession`, `@Generable enum GenKind`, 제목 입력, 본문 1,500자 절단, `classifyDetailed(...) async -> FMOutcome`(verdict/unavailable/timeout/error, throw 없음), `classify(...) async -> FMVerdict?`, 응답·타이머 경합으로 제시간 타임아웃. 폴백 라우팅은 `CapturePipeline.route`(카톡·인스타 폐기, 그 외 `device_filter="rules"`). 벤치마크는 1건 사전 점검 후 실행, 행별 에러를 세고 앱별 개인 대화 통과율을 낸다. 픽스처는 중복 제거·재균형. 판정 상태: PoC-3 **부분**(호스트 Mac Apple Intelligence 꺼짐으로 시뮬레이터 `respond` 에셋 오류, `availability()`는 available 오표시). 검진 결과 안내 4건 라벨은 `medical_result`로 바꿨다(픽스처 personal 100·notice 96·medical_result 4, 벤치마크는 `medicalDrop=x/4`로 따로 셈, `@Guide`에 결과 안내·예약 안내 경계 추가. 스펙 §16 코드 수정 필요 #4 완료).

**Files:**
- Create: `.../Sources/AssistantCore/FMClassifier.swift`, `.../Tests/AssistantCoreTests/FMClassifierTests.swift`, `.../Tests/AssistantCoreTests/Fixtures/notifications.json`
- Modify: `poc/ios/App/CaptureIntent.swift` (규칙 통과 후 FM 호출)

**Interfaces:**
- Produces:
```swift
public enum NoticeKind: String, Codable, CaseIterable { case notice, personal, otp, promo, medical_result }
public struct FMVerdict: Equatable { public let kind: NoticeKind; public let confidence: Double }
public enum FMOutcome: Equatable, Sendable { case verdict(FMVerdict), unavailable(String), timeout, error(String) }  // 구현에서 추가
public actor FMClassifier {
  public static func availability() -> String   // "available" | "deviceNotEligible" | "appleIntelligenceNotEnabled" | "modelNotReady"
  public init()
  // 구현: throw 하지 않는다. nil = 불가·타임아웃·생성 에러 (스펙 §6 폴백)
  public func classify(text: String, appName: String?, title: String? = nil, timeout: Duration = .seconds(3)) async -> FMVerdict?
  public func classifyDetailed(text: String, appName: String?, title: String? = nil, timeout: Duration = .seconds(3)) async -> FMOutcome
}
```

- [x] **Step 1: 픽스처 작성**

`Fixtures/notifications.json`: 200건. 형식 `[{"app":"KakaoTalk","title":"쿠팡","text":"...","expected":"notice"}]`. 구성: 알림톡 100건(주문·배송·예약·결제·병원·택배, 실제 문구 변형), 개인 대화 100건(존댓말·반말·이모지·짧은 답장·"예약했어?" 같이 키워드 포함된 사적 대화 20건 이상). 작성자가 직접 만들고 AI가 생성한 문장은 절반 이하로 둔다.

- [x] **Step 2: 실패하는 테스트**

```swift
import XCTest
@testable import AssistantCore
final class FMClassifierTests: XCTestCase {
  struct Row: Decodable { let app: String; let title: String?; let text: String; let expected: String }
  func testBenchmark() async throws {
    try XCTSkipUnless(FMClassifier.availability() == "available", "FM unavailable: \(FMClassifier.availability())")
    let url = Bundle(for: Self.self).url(forResource: "notifications", withExtension: "json")!
    let rows = try JSONDecoder().decode([Row].self, from: Data(contentsOf: url))
    let c = FMClassifier()
    var personalPassed = 0, noticeDropped = 0, personalTotal = 0, noticeTotal = 0
    var latencies: [Double] = []
    for r in rows {
      let t0 = Date()
      let v = try await c.classify(text: r.text, appName: r.app)
      latencies.append(Date().timeIntervalSince(t0))
      let kind = v?.kind ?? .personal   // nil이면 폐기 취급
      if r.expected == "personal" { personalTotal += 1; if kind == .notice { personalPassed += 1 } }
      if r.expected == "notice" { noticeTotal += 1; if kind != .notice { noticeDropped += 1 } }
    }
    let p95 = latencies.sorted()[Int(Double(latencies.count) * 0.95)]
    let report = "p95=\(p95)s personalPass=\(personalPassed)/\(personalTotal) noticeDrop=\(noticeDropped)/\(noticeTotal)"
    print("FM_BENCH", report)
    try report.write(to: AppGroup.containerURL().appendingPathComponent("fm_bench.txt"), atomically: true, encoding: .utf8)
    XCTAssertLessThan(p95, 3.0)
    XCTAssertLessThanOrEqual(Double(personalPassed) / Double(personalTotal), 0.02)
    XCTAssertLessThanOrEqual(Double(noticeDropped) / Double(noticeTotal), 0.15)
  }
}
```

- [x] **Step 3: 구현**

```swift
import Foundation
import FoundationModels

public enum NoticeKind: String, Codable, CaseIterable { case notice, personal, otp, promo, medical_result }
public struct FMVerdict: Equatable { public let kind: NoticeKind; public let confidence: Double }

@Generable
struct GenVerdict {
  @Guide(description: "notice=기업·기관의 자동 알림(주문,배송,예약,결제,병원 안내). personal=사람이 사람에게 쓴 대화. otp=인증번호. promo=광고. medical_result=검사 결과·진단 내용")
  var kind: String
  @Guide(description: "0.0~1.0") var confidence: Double
}

public actor FMClassifier {
  public static func availability() -> String {
    switch SystemLanguageModel.default.availability {
    case .available: return "available"
    case .unavailable(.deviceNotEligible): return "deviceNotEligible"
    case .unavailable(.appleIntelligenceNotEnabled): return "appleIntelligenceNotEnabled"
    case .unavailable(.modelNotReady): return "modelNotReady"
    case .unavailable(_): return "unavailable"
    }
  }
  private let session: LanguageModelSession
  public init() {
    session = LanguageModelSession(instructions: """
      너는 한국어 휴대폰 알림을 분류한다. 알림 텍스트만 보고 kind를 정한다.
      사람 사이의 대화(약속 잡기, 안부, 질문)는 키워드가 무엇이든 personal이다.
      기업·기관·봇이 보낸 정형 안내는 notice다.
      """)
  }
  public func classify(text: String, appName: String?, timeout: Duration = .seconds(3)) async throws -> FMVerdict? {
    let prompt = "앱: \(appName ?? "unknown")\n알림: \(text)"
    return try await withThrowingTaskGroup(of: FMVerdict?.self) { g in
      g.addTask { [session] in
        let r = try await session.respond(to: prompt, generating: GenVerdict.self)
        return FMVerdict(kind: NoticeKind(rawValue: r.content.kind) ?? .personal, confidence: r.content.confidence)
      }
      g.addTask { try await Task.sleep(for: timeout); return nil }
      let first = try await g.next() ?? nil
      g.cancelAll()
      return first
    }
  }
}
```
~~주의: `LanguageModelSession`은 동시 요청을 허용하지 않으므로 actor로 직렬화한다.~~ **이 전제는 틀렸다(구현에서 폐기).** actor 메서드 안의 `await`는 재진입 지점이라 직렬화되지 않고, 세션 하나를 재사용하면 transcript가 누적돼 벤치마크 도중 4,096토큰을 넘는다. 호출마다 새 `LanguageModelSession`을 만든다. 또 `withThrowingTaskGroup`은 남은 자식을 기다리므로 `respond`가 취소에 협조하지 않으면 타임아웃이 늦어진다. 응답·타이머 중 먼저 끝난 쪽이 continuation을 재개하는 방식으로 바꿨다(스펙 §6 "Foundation Models 분류").

- [x] **Step 4: 시뮬레이터에서 가용성 확인**

`sim.sh test AssistantCoreTests/FMClassifierTests`. 시뮬레이터에서 `availability()`가 `available`이 아니면(Mac의 Apple Intelligence 꺼짐 등) 테스트는 스킵되고 사유가 출력된다. 이 경우 Mac 시스템 설정 → Apple Intelligence를 켜고 재시도. 그래도 불가하면 **실기기**에서 같은 테스트를 돌린다(Xcode에서 실기기 선택 후 ⌘U).

실측(09-24): 호스트 Mac이 `appleIntelligenceNotEnabled`인데도 시뮬레이터 `availability()`는 `available`을 반환했고 `respond`는 에셋 오류를 냈다. 그래서 벤치마크는 `availability()`만 믿지 않고 **1건 사전 점검**(짧은 합성 문장, 타임아웃 30초)이 에러면 `XCTSkip`한다. 시뮬레이터 수치는 참고용이며 판정은 실기기 수치로 한다(스펙 §16).

- [x] **Step 5: CaptureIntent에 FM 단계 연결**

`CapturePipeline`에 두 메서드를 추가한다(기존 `handle`은 유지):
```swift
public func filterOnly(text: String, sender: String?) -> RuleVerdict { filter.apply(text: text, sender: sender) }
public func enqueue(source: String, appName: String?, title: String?, sender: String?, masked: String) throws {
  try queue.enqueue(CaptureItem(id: UUID().uuidString, source: source, appName: appName, sender: sender, title: title,
                                text: masked, localFile: nil, ocrText: nil, capturedAt: Date(), attempts: 0))
}
```
`CaptureIntent.perform` 본문을 다음으로 교체:
```swift
let pipeline = CapturePipeline(filter: RuleFilter(), queue: try CaptureQueue.shared())
guard case .pass(let masked) = pipeline.filterOnly(text: text, sender: sender) else {
  PoCLog.append("rule discard"); return .result(dialog: "discarded:rule")
}
let isChatApp = ["KakaoTalk", "카카오톡", "Instagram"].contains(appName ?? "")
if FMClassifier.availability() == "available" {
  if let v = try await FMClassifier().classify(text: masked, appName: appName) {
    if v.kind != .notice { PoCLog.append("FM discard \(v.kind) \(v.confidence)"); return .result(dialog: "discarded:fm:\(v.kind.rawValue)") }
  } else if isChatApp { return .result(dialog: "discarded:fm-timeout") }
} else if isChatApp {
  return .result(dialog: "discarded:fm-unavailable")
}
try pipeline.enqueue(source: source, appName: appName, title: title, sender: sender, masked: masked)
PoCLog.append("CaptureIntent queued")
return .result(dialog: "queued")
```

- [x] **Step 6: 결과 기록·커밋**

`fm_bench.txt` 수치를 `docs/superpowers/poc/results.md` PoC-3 행에 적는다. `git commit -m "poc(core): Foundation Models classifier with 200-sample benchmark"`

---

### Task 6: 알림 액션 → 백그라운드 EventKit 멱등 쓰기 (PoC-5)

> **구현 결과 (완료):** 커밋 `0e89279`, 리뷰 반영 `0d2a293`. 원안의 델리게이트 핸들러는 확인과 쓰기 사이에 경쟁이 있어 동시에 두 번 탭하면 이벤트가 2건 생길 수 있었다. 현재: 확인 → 저장 → 기록을 `actor AddEventGate` 안에서 `await` 없이 처리(스펙 §10), 이벤트 `url`에 `assistant://proposal/<id>` 표식, 로그에 `bg=`·`auth=`(권한 상태). APNs 없이 배너·액션 경로를 타는 로컬 알림 `NotificationActions.scheduleLocal`. Step 7 시나리오는 XCUITest(`testBannerActionInBackgroundThenDuplicate`, `testColdStartFromBannerAction`)와 동시 탭 훅으로 수행: `ADD ok … bg=true`, `dup skip`, 앱 종료 후 콜드 스타트 `bg=true`, 동시 두 번 탭 이벤트 +1. 판정 상태: PoC-5 **부분**(잠금 화면 `.authenticationRequired`는 실기기).

**Files:**
- Create: `poc/ios/App/NotificationActions.swift`, `.../Sources/AssistantCore/Executions.swift`, `.../Tests/AssistantCoreTests/ExecutionsTests.swift`, `poc/ios/scripts/push.sh`, `poc/ios/scripts/payloads/add_event.apns`
- Modify: `poc/ios/App/AssistantPoCApp.swift` (델리게이트 등록, 권한 요청)

**Interfaces:**
- Consumes: `AppGroup`
- Produces:
```swift
public final class Executions {          // App Group SQLite, 테이블 executions(proposal_id PK, eventkit_id, executed_at, reported INTEGER)
  public init(url: URL) throws; public static func shared() throws -> Executions
  public func existing(proposalId: String) throws -> String?          // eventkit_id
  public func record(proposalId: String, eventkitId: String) throws
  public func unreported() throws -> [(proposalId: String, eventkitId: String)]
  public func markReported(proposalId: String) throws
}
```

- [x] **Step 1: Executions 실패하는 테스트**

```swift
import XCTest
@testable import AssistantCore
final class ExecutionsTests: XCTestCase {
  func make() throws -> Executions { try Executions(url: FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString)) }
  func testRecordThenExisting() throws {
    let e = try make(); try e.record(proposalId: "p1", eventkitId: "ek1")
    XCTAssertEqual(try e.existing(proposalId: "p1"), "ek1")
  }
  func testDuplicateRecordKeepsFirst() throws {
    let e = try make(); try e.record(proposalId: "p1", eventkitId: "ek1"); try e.record(proposalId: "p1", eventkitId: "ek2")
    XCTAssertEqual(try e.existing(proposalId: "p1"), "ek1")
    XCTAssertEqual(try e.unreported().count, 1)
  }
  func testMarkReported() throws {
    let e = try make(); try e.record(proposalId: "p1", eventkitId: "ek1"); try e.markReported(proposalId: "p1")
    XCTAssertTrue(try e.unreported().isEmpty)
  }
}
```

- [x] **Step 2: 실행해 실패 확인** → "cannot find 'Executions'"

- [x] **Step 3: 구현**

```swift
import Foundation
import SQLite3

public final class Executions {
  public enum Error: Swift.Error { case sqlite(String) }
  private var db: OpaquePointer?
  private static let transient = unsafeBitCast(-1, to: sqlite3_destructor_type.self)
  private var msg: String { String(cString: sqlite3_errmsg(db)) }

  public init(url: URL) throws {
    guard sqlite3_open_v2(url.path, &db, SQLITE_OPEN_READWRITE | SQLITE_OPEN_CREATE | SQLITE_OPEN_FULLMUTEX, nil) == SQLITE_OK else { throw Error.sqlite(String(cString: sqlite3_errmsg(db))) }
    guard sqlite3_exec(db, "CREATE TABLE IF NOT EXISTS executions(proposal_id TEXT PRIMARY KEY, eventkit_id TEXT NOT NULL, executed_at REAL NOT NULL, reported INTEGER NOT NULL DEFAULT 0)", nil, nil, nil) == SQLITE_OK else { throw Error.sqlite(msg) }
  }
  deinit { sqlite3_close(db) }
  public static func shared() throws -> Executions { try Executions(url: try AppGroup.containerURL().appendingPathComponent("executions.sqlite")) }

  public func existing(proposalId: String) throws -> String? {
    var s: OpaquePointer?; defer { sqlite3_finalize(s) }
    guard sqlite3_prepare_v2(db, "SELECT eventkit_id FROM executions WHERE proposal_id=?", -1, &s, nil) == SQLITE_OK else { throw Error.sqlite(msg) }
    sqlite3_bind_text(s, 1, proposalId, -1, Self.transient)
    return sqlite3_step(s) == SQLITE_ROW ? String(cString: sqlite3_column_text(s, 0)) : nil
  }
  public func record(proposalId: String, eventkitId: String) throws {
    try run("INSERT OR IGNORE INTO executions(proposal_id,eventkit_id,executed_at) VALUES(?,?,?)") { s in
      sqlite3_bind_text(s, 1, proposalId, -1, Self.transient); sqlite3_bind_text(s, 2, eventkitId, -1, Self.transient); sqlite3_bind_double(s, 3, Date().timeIntervalSince1970)
    }
  }
  public func unreported() throws -> [(proposalId: String, eventkitId: String)] {
    var s: OpaquePointer?; defer { sqlite3_finalize(s) }
    guard sqlite3_prepare_v2(db, "SELECT proposal_id, eventkit_id FROM executions WHERE reported=0", -1, &s, nil) == SQLITE_OK else { throw Error.sqlite(msg) }
    var out: [(String, String)] = []
    while sqlite3_step(s) == SQLITE_ROW { out.append((String(cString: sqlite3_column_text(s, 0)), String(cString: sqlite3_column_text(s, 1)))) }
    return out.map { (proposalId: $0.0, eventkitId: $0.1) }
  }
  public func markReported(proposalId: String) throws {
    try run("UPDATE executions SET reported=1 WHERE proposal_id=?") { sqlite3_bind_text($0, 1, proposalId, -1, Self.transient) }
  }
  private func run(_ sql: String, bind: (OpaquePointer) -> Void) throws {
    var s: OpaquePointer?
    guard sqlite3_prepare_v2(db, sql, -1, &s, nil) == SQLITE_OK, let st = s else { throw Error.sqlite(msg) }
    defer { sqlite3_finalize(st) }
    bind(st)
    guard sqlite3_step(st) == SQLITE_DONE else { throw Error.sqlite(msg) }
  }
}
```

- [x] **Step 4: 테스트 통과 확인** → 3 passed

- [x] **Step 5: 알림 카테고리와 핸들러**

```swift
import UserNotifications
import EventKit
import AssistantCore

enum NotificationActions {
  static let category = "ADD_EVENT"
  static func register() {
    let add = UNNotificationAction(identifier: "ADD", title: "캘린더에 추가", options: [.authenticationRequired])
    let ignore = UNNotificationAction(identifier: "IGNORE", title: "무시", options: [])
    let cat = UNNotificationCategory(identifier: category, actions: [add, ignore], intentIdentifiers: [])
    UNUserNotificationCenter.current().setNotificationCategories([cat])
  }
}

final class NotificationDelegate: NSObject, UNUserNotificationCenterDelegate {
  func userNotificationCenter(_ c: UNUserNotificationCenter, didReceive r: UNNotificationResponse) async {
    guard r.actionIdentifier == "ADD" else { return }
    let info = r.notification.request.content.userInfo
    guard let pid = info["proposal_id"] as? String, let title = info["title"] as? String,
          let startISO = info["start"] as? String, let start = ISO8601DateFormatter().date(from: startISO) else { return }
    do {
      let ex = try Executions.shared()
      if let existing = try ex.existing(proposalId: pid) { PoCLog.append("ADD dup skip \(pid) \(existing)"); return }
      let store = EKEventStore()
      let ev = EKEvent(eventStore: store)
      ev.title = title; ev.startDate = start; ev.endDate = start.addingTimeInterval(3600)
      ev.calendar = store.defaultCalendarForNewEvents
      try store.save(ev, span: .thisEvent, commit: true)
      try ex.record(proposalId: pid, eventkitId: ev.eventIdentifier)
      PoCLog.append("ADD ok \(pid) \(ev.eventIdentifier) bg=\(UIApplication.shared.applicationState == .background)")
    } catch { PoCLog.append("ADD fail \(pid) \(error)") }
  }
}
```
앱 시작 시 `UNUserNotificationCenter.current().delegate = delegate`, `requestAuthorization([.alert,.sound])`, `EKEventStore().requestFullAccessToEvents()`를 ContentView 버튼으로 호출.

- [x] **Step 6: simctl push 페이로드와 스크립트**

`payloads/add_event.apns`:
```json
{ "Simulator Target Bundle": "com.picpal.assistant.poc",
  "aps": { "alert": { "title": "일정 제안", "body": "9월 25일 15:00 병원 예약" }, "category": "ADD_EVENT" },
  "proposal_id": "p-001", "title": "병원 예약", "start": "2026-09-25T15:00:00+09:00" }
```
`push.sh`: `xcrun simctl push "$(cat .sim-udid)" com.picpal.assistant.poc scripts/payloads/${1:-add_event}.apns`

- [x] **Step 7: 시뮬레이터 시나리오**

1. 앱 실행 → 권한 2개 허용 → 홈으로 나감(백그라운드).
2. `push.sh` → 알림 길게 눌러 "캘린더에 추가". 캘린더 앱에 1건 생성, poc.log에 `ADD ok ... bg=true`.
3. 같은 페이로드 다시 push → "추가" → poc.log `ADD dup skip`, 캘린더 여전히 1건.
4. 앱을 스와이프로 종료한 뒤 push → "추가" → 생성되는지(콜드 스타트 백그라운드 실행).
결과를 results.md PoC-5에 기록. 잠금 상태 + authenticationRequired 동작은 **실기기**에서 1·2를 반복해 기록한다.

- [x] **Step 8: 커밋** `git commit -m "poc(ios): notification action -> idempotent EventKit write"`

---

### Task 7: Share Extension 로컬 영속화 + 백그라운드 업로드 (PoC-8 기기 부분, PoC-9)

> **구현 결과 (완료):** 커밋 `f28814d`, 리뷰 반영 `0d2a293`. 원안과 차이: 목 서버 포트를 `MOCK_INGEST_PORT`(기본 8787)로 바꿀 수 있게 했고, 이 기계는 8787·8788이 무관한 프로세스에 점유돼 **8799**로 실측했다(`SIMCTL_CHILD_INGEST_URL=http://localhost:8799`). `MOCK_INGEST_SLOW_MS`(기본 0)는 업로드 바디를 청크마다 늦게 읽어 "전송 도중 앱 종료"를 재현하는 PoC-9 전용 옵션. `Uploader`는 `@unchecked Sendable` 없이 `let session` + 별도 `UploadDelegate`, flush는 `pending` 대신 `claim`(lease)으로 중복 업로드를 막는다. 판정 상태: PoC-9 **통과**(앱 프로세스 종료 확인 후 3.68초 뒤 정확한 바이트 도착), PoC-8 기기 부분 **부분**(사진 앱 공유 시트 XCUITest `testPhotosShareSheetRunsExtension`, `ocrLen=43`). Share Extension 규칙 필터는 적용했다: 텍스트·URL·OCR에 `RuleFilter`, OTP 폐기면 큐·파일 모두 남기지 않음(임시 복사 → OCR → 규칙 → 영속화 → 큐, 스펙 §6·§16 코드 수정 필요 #2 완료). 업로드 서버 주소는 앱 화면 입력란(App Group `ingestURL`)이 우선이다.

**Files:**
- Create: `poc/ios/ShareExtension/ShareViewController.swift`, `poc/ios/ShareExtension/Info.plist`, `poc/ios/ShareExtension/ShareExtension.entitlements`, `poc/ios/App/Uploader.swift`, `.../Sources/AssistantCore/OCR.swift`, `poc/server/mock-ingest.ts`

**Interfaces:**
- Consumes: `CaptureQueue`, `AppGroup`
- Produces: `Uploader.shared.flush()` — 큐의 pending을 `POST $INGEST_URL/ingest`(JSON) 또는 파일이 있으면 `PUT $INGEST_URL/upload/<id>`로 background URLSession 전송. `OCR.recognize(imageURL:) async throws -> String` (Vision, ko-KR).

- [x] **Step 1: 목 수신 서버 (deno)**

`poc/server/mock-ingest.ts`:
```ts
// 구현: 포트는 MOCK_INGEST_PORT(기본 8787), MOCK_INGEST_SLOW_MS 로 업로드 바디를 천천히 읽는 옵션 추가 (poc/server/mock-ingest.ts)
const received: Record<string, unknown>[] = [];
Deno.serve({ port: 8787 }, async (req) => {
  const u = new URL(req.url);
  if (u.pathname === "/ingest") { received.push(await req.json()); return new Response("ok", { status: 202 }); }
  if (u.pathname.startsWith("/upload/")) { const b = await req.arrayBuffer(); received.push({ file: u.pathname, bytes: b.byteLength }); return new Response("ok"); }
  if (u.pathname === "/received") return Response.json(received);
  return new Response("nf", { status: 404 });
});
```
실행: `deno run --allow-net --allow-env poc/server/mock-ingest.ts`. 시뮬레이터에서는 `http://localhost:8787` 접근 가능. Info.plist에 `NSAppTransportSecurity > NSAllowsLocalNetworking = true`.

이 기계에서는 8787·8788이 무관한 프로세스에 점유돼 있어 **8799**를 쓴다(점유 프로세스는 건드리지 않는다): `MOCK_INGEST_PORT=8799 deno run --allow-net --allow-env poc/server/mock-ingest.ts`, 앱은 `SIMCTL_CHILD_INGEST_URL=http://localhost:8799 xcrun simctl launch …`. 전송 도중 앱 종료를 재현할 때는 `MOCK_INGEST_SLOW_MS=200`처럼 지연을 준다.

업로드 서버 주소는 이후 앱 화면 "업로드 서버" 입력란에서 저장하는 값(App Group `UserDefaults` 키 `ingestURL`)이 우선이다. `INGEST_URL`(스킴 환경변수·`SIMCTL_CHILD_INGEST_URL`)은 저장값이 없을 때만 초기값으로 쓰이고, 둘 다 없으면 `http://localhost:8787`(스펙 §6 업로더).

- [x] **Step 2: Share Extension**

`ShareExtension/Info.plist`의 `NSExtensionActivationRule`: text·URL·image·PDF 각 1개 허용. 엔타이틀먼트에 같은 App Group.

`ShareViewController.swift`:
```swift
import UIKit
import UniformTypeIdentifiers
import AssistantCore

final class ShareViewController: UIViewController {
  override func viewDidLoad() {
    super.viewDidLoad()
    Task { await handle(); extensionContext?.completeRequest(returningItems: nil) }
  }
  private func handle() async {
    guard let items = extensionContext?.inputItems as? [NSExtensionItem] else { return }
    for item in items { for p in item.attachments ?? [] {
      if p.hasItemConformingToTypeIdentifier(UTType.image.identifier) || p.hasItemConformingToTypeIdentifier(UTType.pdf.identifier) {
        let type = p.hasItemConformingToTypeIdentifier(UTType.pdf.identifier) ? UTType.pdf : UTType.image
        guard let src = try? await p.loadFileURL(type) else { continue }
        let id = UUID().uuidString
        let dir = try! AppGroup.containerURL().appendingPathComponent("inbox", isDirectory: true)
        try? FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true)
        let dst = dir.appendingPathComponent(id + "." + (type == .pdf ? "pdf" : "jpg"))
        try? FileManager.default.copyItem(at: src, to: dst)            // 1. 먼저 영속화
        let ocr = type == .image ? (try? await OCR.recognize(imageURL: dst)) ?? "" : ""
        try? CaptureQueue.shared().enqueue(CaptureItem(id: id, source: "SHARE", appName: nil, sender: nil, title: nil,
          text: "", localFile: "inbox/" + dst.lastPathComponent, ocrText: ocr, capturedAt: Date(), attempts: 0))
      } else if p.hasItemConformingToTypeIdentifier(UTType.url.identifier), let url = try? await p.loadItem(forTypeIdentifier: UTType.url.identifier) as? URL {
        try? CaptureQueue.shared().enqueue(CaptureItem(id: UUID().uuidString, source: "SHARE", appName: nil, sender: nil, title: nil, text: url.absoluteString, localFile: nil, ocrText: nil, capturedAt: Date(), attempts: 0))
      } else if p.hasItemConformingToTypeIdentifier(UTType.plainText.identifier), let s = try? await p.loadItem(forTypeIdentifier: UTType.plainText.identifier) as? String {
        try? CaptureQueue.shared().enqueue(CaptureItem(id: UUID().uuidString, source: "SHARE", appName: nil, sender: nil, title: nil, text: s, localFile: nil, ocrText: nil, capturedAt: Date(), attempts: 0))
      }
    } }
  }
}
extension NSItemProvider {
  func loadFileURL(_ type: UTType) async throws -> URL {
    try await withCheckedThrowingContinuation { c in
      loadFileRepresentation(forTypeIdentifier: type.identifier) { url, err in
        if let url { let tmp = FileManager.default.temporaryDirectory.appendingPathComponent(url.lastPathComponent)
          try? FileManager.default.removeItem(at: tmp); try? FileManager.default.copyItem(at: url, to: tmp); c.resume(returning: tmp) }
        else { c.resume(throwing: err ?? CocoaError(.fileNoSuchFile)) }
      }
    }
  }
}
```

`OCR.swift`:
```swift
import Vision
public enum OCR {
  public static func recognize(imageURL: URL) async throws -> String {
    let req = VNRecognizeTextRequest(); req.recognitionLanguages = ["ko-KR", "en-US"]; req.recognitionLevel = .accurate
    try VNImageRequestHandler(url: imageURL).perform([req])
    return (req.results ?? []).compactMap { $0.topCandidates(1).first?.string }.joined(separator: "\n")
  }
}
```

- [x] **Step 3: Uploader (background URLSession)**

```swift
import Foundation
import AssistantCore

final class Uploader: NSObject, URLSessionDelegate, URLSessionTaskDelegate {
  static let shared = Uploader()
  static let base = URL(string: ProcessInfo.processInfo.environment["INGEST_URL"] ?? "http://localhost:8787")!
  lazy var session: URLSession = {
    let c = URLSessionConfiguration.background(withIdentifier: "com.picpal.assistant.poc.upload")
    c.sharedContainerIdentifier = AppGroup.id
    return URLSession(configuration: c, delegate: self, delegateQueue: nil)
  }()
  func flush() {
    guard let q = try? CaptureQueue.shared(), let items = try? q.pending(limit: 20) else { return }
    for it in items {
      if let rel = it.localFile, let container = try? AppGroup.containerURL() {
        var r = URLRequest(url: Self.base.appendingPathComponent("upload/\(it.id)")); r.httpMethod = "PUT"
        let t = session.uploadTask(with: r, fromFile: container.appendingPathComponent(rel)); t.taskDescription = it.id; t.resume()
      } else {
        var r = URLRequest(url: Self.base.appendingPathComponent("ingest")); r.httpMethod = "POST"
        r.setValue("application/json", forHTTPHeaderField: "Content-Type")
        let body = try! JSONEncoder().encode(it)
        let tmp = FileManager.default.temporaryDirectory.appendingPathComponent(it.id + ".json"); try! body.write(to: tmp)
        let t = session.uploadTask(with: r, fromFile: tmp); t.taskDescription = it.id; t.resume()
      }
    }
  }
  func urlSession(_ s: URLSession, task: URLSessionTask, didCompleteWithError error: Error?) {
    guard let id = task.taskDescription, let q = try? CaptureQueue.shared() else { return }
    if error == nil, (task.response as? HTTPURLResponse).map({ (200..<300).contains($0.statusCode) }) == true {
      try? q.markSent(id: id); PoCLog.append("upload ok \(id)")
    } else { try? q.markFailed(id: id); PoCLog.append("upload fail \(id) \(String(describing: error))") }
  }
}
```
앱 시작·`scenePhase == .active` 전환 시 `Uploader.shared.flush()` 호출.

- [x] **Step 4: 시뮬레이터 시나리오 (PoC-8 기기 부분, PoC-9)**

1. 목 서버 실행. 사진 앱에 청첩장 이미지 1장 드래그 → 공유 → AssistantPoC. 앱 열기 → 큐에 localFile·ocrText 있음 → flush → `curl localhost:8787/received`에 bytes 항목.
2. 목 서버를 끈 상태에서 공유 → 앱 열어 flush → 실패 기록 → 목 서버 켜고 앱 재진입 → 성공. 유실 0 확인.
3. 공유 직후 앱을 스와이프 종료 → 재실행 → 큐 항목 남아 있고 전송됨.
4. 시뮬레이터 네트워크 차단(`Settings → Developer → Network Link Conditioner 100% loss`) 후 flush → 복구 후 자동 완료되는지, 안 되면 재진입 시 flush로 완료되는지 기록.

- [x] **Step 5: 커밋** `git commit -m "poc(ios): share extension with local persistence, OCR, background uploader"`

---

### 기기 PoC 실측 도구 (Task 1~7에서 추가, 재실측에 사용)

| 도구 | 용도 | 검증 범위 |
|---|---|---|
| `sim.sh uitest [AssistantPoCUITests/SimRemeasureUITests/<테스트>]` | 알림 권한 대화상자·배너 액션·단축어 앱·사진 공유 시트를 springboard로 조작 | 실제 OS 경로. 시뮬레이터 실측의 주 수단 |
| `sim.sh log [n]` | App Group `poc.log` 끝부분 | — |
| 앱 "업로드 서버" 입력란 | 목 서버 주소 저장(App Group `ingestURL`). 앱 시작 시 `ingest base=<url>`, 업로드 시 `flush claimed N to=<host>:<port>` 로그로 확인. 목 서버는 `deno run --allow-net --allow-env poc/server/mock-ingest.ts` | 스킴 `INGEST_URL`은 저장값이 없을 때만 초기값 |
| `--poc-debug-capture` | `CapturePipeline.handle`(규칙 → 큐)만 실행 | 인텐트·FM·단축어 경로를 거치지 않음 |
| `--poc-debug-capture-intent=<app> [--poc-debug-title=<t>] [--poc-debug-sender=<s>]` | 앱 프로세스에서 `CaptureIntent.perform()` 전체(규칙 → FM → 라우팅 → 큐). 제목·발신자 인자는 연락처 규칙 실측용 | 단축어 호출 경로는 아님 |
| `--poc-debug-notification-action [--proposal-id=]` | `handleAdd` 직접 호출 | 배너·액션 탭·백그라운드 실행은 아님 |
| `--poc-debug-notification-action-concurrent [--proposal-id=]` | 같은 proposal_id로 `handleAdd` 동시 2회 | 직렬 구간 검증 |
| `--poc-debug-local-notification[=<초>] [--proposal-id=]` | 로컬 알림 예약(기본 10초). APNs 없이 배너·액션 경로 | 실제 배너 경로 |
| `--poc-debug-count-events` | 9/25 "병원 예약" 이벤트 수를 EventKit으로 독립 조회 | — |
| `--poc-debug-share-image=<호스트 경로>` | 확장과 같은 영속화 → OCR → 큐 호출 | 공유 시트·확장 프로세스는 아님 |

디버그 훅으로만 확인한 항목은 results.md에서 **부분**이다. 훅은 PoC 앱에만 있고 제품 코드에 옮기지 않는다.

---

### Task 8: Supabase 프로젝트 + jobs 영속 큐 + 봉투 암호화 + worker (PoC-10)

**Files:**
- Create: `poc/server/supabase/config.toml`(CLI 생성), `poc/server/supabase/migrations/0001_jobs.sql`, `poc/server/supabase/migrations/0002_cron.sql`, `poc/server/supabase/migrations/0003_items_keys.sql`, `poc/server/supabase/functions/_shared/job.ts`, `poc/server/supabase/functions/_shared/crypto.ts`, `poc/server/supabase/functions/_shared/rules.ts`, `poc/server/supabase/functions/ingest/handler.ts`, `poc/server/supabase/functions/ingest/index.ts`, `poc/server/supabase/functions/worker/index.ts`, `poc/server/supabase/functions/worker/process.ts`, `poc/server/supabase/tests/jobs.test.ts`, `poc/server/supabase/tests/crypto.test.ts`, `poc/server/supabase/tests/rules.test.ts`, `poc/server/supabase/tests/ingest.test.ts`, `poc/server/scripts/seed-items.ts`, `poc/server/.env.example`

**Interfaces:**
- Produces:
  - 테이블 `jobs(id uuid pk, kind text, user_id uuid null, payload jsonb, lease_key text, leased_until timestamptz, attempts int, status text, checkpoint text, last_error text, created_at, updated_at)`
  - 함수 `claim_jobs(p_limit int, p_lease_seconds int) returns setof jobs`(lease_key당 1건만), `complete_job(p_id uuid, p_checkpoint text)`, `fail_job(p_id uuid, p_error text)`, `enqueue_job(p_user uuid, p_kind text, p_lease_key text, p_payload jsonb) returns uuid`
  - 테이블 `user_keys(user_id pk, wrapped_key bytea, created_at)`, `items`(스펙 §8 컬럼, 원문은 `content_enc bytea`·`ocr_text_enc bytea`), `audit_log(user_id, actor, action, target, at)`
  - 함수 `insert_item(p_user, p_source, p_idempotency_key, p_sender, p_title, p_content_enc bytea, p_occurred_at, p_enqueue boolean default true, p_app_name text default null, p_ocr_text_enc bytea default null) returns uuid`(중복이면 null, `p_enqueue`면 `jobs(kind='process')` 동시 삽입), `worker_get_item(p_user uuid, p_item uuid) returns table(id, content_enc, ocr_text_enc)`(소유자 불일치면 0행, 반환 시 `audit_log(action='decrypt')`), `get_wrapped_key(p_user) returns bytea`, `put_wrapped_key(p_user, p_wrapped bytea) returns bytea`
  - `_shared/crypto.ts`: `encrypt(userId: string, plaintext: string): Promise<Uint8Array>`, `decrypt(userId: string, data: Uint8Array | string): Promise<string>`, `toBytea(b: Uint8Array): string`, `fromBytea(v: Uint8Array | string): Uint8Array`, `createEnvelope(store: KeyStore, masterKeyB64: string)`
  - `_shared/job.ts`: `type Job`. `worker/process.ts`: `processItem(sb, job, extract: Extract): Promise<string>`
  - `_shared/rules.ts`: `applyRules(text: string, meta?: { sender?; title?; labels? }): RuleVerdict`(`{ kind: "discard"; reason: "otp" | "promotion" } | { kind: "pass"; masked: string }`), `maskSensitive(s: string): string`, `isOtp`, `luhn`. 기기 `RuleFilter`(Task 2)와 같은 정규식
  - Edge `ingest`: `POST /functions/v1/ingest` body = 기기 `CaptureItem` JSON(Task 3) → 401(JWT 없음·무효) / 400(형식) / 204(규칙 폐기, 저장 없음) / 202 `{ item_id, duplicate }`. 순수 핸들러 `handleIngest(req, deps: IngestDeps)`
  - Edge `worker`가 매분 호출됨. 핸들러 맵 `handlers: Record<string, (job: Job) => Promise<string>>`

**보안 경계 (스펙 §12 통제 1·4와의 관계):**
- 모든 테이블에 RLS를 켜고 소유자 읽기 정책 `(select auth.uid()) = user_id`만 둔다. 쓰기 정책은 없다. 앱 클라이언트 경로 격리용이다.
- 워커·웹훅(service_role)은 RLS를 우회하므로 `items`·`user_keys`는 위 저장 프로시저로만 다루고 모든 호출에 `p_user`를 넘긴다. 저장 프로시저는 `anon`·`authenticated`에서 실행 권한을 회수한다.
- 복호화는 Edge Function 메모리에서만 한다. 마스터 키는 Edge secret `MASTER_KEY`에만 있고 DB에는 없다. 평문·오류 원문은 `console.log`에 남기지 않고 item_id·길이·소요 시간만 남긴다.
- **PoC 한정 예외**: 이 프로젝트는 합성 데이터만 담는 폐기용 프로젝트다. 테스트·스크립트를 돌리려고 `SUPABASE_SERVICE_ROLE_KEY`와 `MASTER_KEY`를 로컬 `.env`에 둔다. 1단계 제품 프로젝트에서는 두 값을 새로 만들고 Edge secret에만 둔다(§12 통제 4(c)). PoC 값을 재사용하지 않는다.

- [ ] **Step 1: 프로젝트 생성·링크·비밀값 (사용자 터미널에서 1회)**

```bash
supabase orgs list                     # org id 확인
supabase projects create assistant-poc --org-id <ORG> --region ap-northeast-2 --db-password '<강한 비밀번호>'
cd poc/server && supabase init && supabase link --project-ref <REF>
MK="$(openssl rand -base64 32)"                     # 봉투 암호화 마스터 키 (PoC 전용)
echo "MASTER_KEY=$MK" >> .env && supabase secrets set MASTER_KEY="$MK" && unset MK
```
`.env.example`: `SUPABASE_URL=`, `SUPABASE_SERVICE_ROLE_KEY=`, `MASTER_KEY=`, `POC_USER_ID=`, `OPENAI_API_KEY=`, `APNS_KEY_ID=`, `APNS_TEAM_ID=6626BYCJG4`, `APNS_P8=`, `APNS_TOPIC=com.picpal.assistant.poc`, `GOOGLE_CLIENT_ID=`, `GOOGLE_CLIENT_SECRET=`, `PUBSUB_SA_EMAIL=`, `GMAIL_PUBSUB_TOPIC=`, `SUPABASE_ANON_KEY=`, `POC_USER_PASSWORD=`(PoC 사용자 로그인용, `openssl rand -base64 18`).

PoC 사용자 1명 생성(`items.user_id`가 `auth.users`를 참조하고, `ingest` 시나리오에서 이 사용자 JWT로 로그인한다). 출력된 UUID를 `.env`의 `POC_USER_ID`에 넣는다:
```bash
deno eval --env-file=.env 'import { createClient } from "npm:@supabase/supabase-js@2";
const sb = createClient(Deno.env.get("SUPABASE_URL"), Deno.env.get("SUPABASE_SERVICE_ROLE_KEY"));
const { data, error } = await sb.auth.admin.createUser({ email: "poc-user@example.com", password: Deno.env.get("POC_USER_PASSWORD"), email_confirm: true });
console.log(error ? error.message : data.user.id);'
```

- [ ] **Step 2: 마이그레이션 — jobs 큐**

`0001_jobs.sql`:
```sql
create extension if not exists pg_cron;
create extension if not exists pg_net;
create table jobs (
  id uuid primary key default gen_random_uuid(),
  kind text not null,
  user_id uuid references auth.users on delete cascade,     -- 시스템 잡(cron 정리 등)은 null
  payload jsonb not null default '{}',
  lease_key text,
  leased_until timestamptz,
  attempts int not null default 0,
  status text not null default 'queued' check (status in ('queued','running','done','dead')),
  checkpoint text,
  last_error text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
create index on jobs (status, created_at) where status = 'queued';
create unique index jobs_one_running_per_key on jobs (lease_key) where status = 'running';
alter table jobs enable row level security;
create policy jobs_owner_read on jobs for select using ((select auth.uid()) = user_id);

-- 동시 호출은 advisory lock으로 직렬화하고, 한 번의 클레임에서도 lease_key당 1건만 고른다.
-- (lease_key가 같은 queued 2건을 한꺼번에 running으로 바꾸면 jobs_one_running_per_key 위반)
create or replace function claim_jobs(p_limit int, p_lease_seconds int)
returns setof jobs language plpgsql as $$
begin
  perform pg_advisory_xact_lock(hashtext('claim_jobs'));
  return query
  with cand as (
    select j.id, j.lease_key, j.status, j.created_at from jobs j
    where (j.status = 'queued' or (j.status = 'running' and j.leased_until < now()))
      and not exists (select 1 from jobs r where r.status = 'running' and r.leased_until >= now()
                      and r.lease_key = j.lease_key and r.id <> j.id)
    order by j.created_at
    for update skip locked
  ), one_per_key as (
    select distinct on (coalesce(lease_key, id::text)) id, created_at from cand
    order by coalesce(lease_key, id::text), (status = 'running') desc, created_at
  )
  update jobs set status = 'running', leased_until = now() + make_interval(secs => p_lease_seconds),
                  attempts = attempts + 1, updated_at = now()
  where id in (select id from one_per_key order by created_at limit p_limit)
  returning *;
end $$;

create or replace function complete_job(p_id uuid, p_checkpoint text) returns void language sql as $$
  update jobs set status = 'done', checkpoint = p_checkpoint, updated_at = now() where id = p_id;
$$;
create or replace function fail_job(p_id uuid, p_error text) returns void language sql as $$
  update jobs set status = case when attempts >= 5 then 'dead' else 'queued' end,
                  last_error = p_error, leased_until = null, updated_at = now() where id = p_id;
$$;
create or replace function enqueue_job(p_user uuid, p_kind text, p_lease_key text, p_payload jsonb) returns uuid
language sql as $$
  insert into jobs (kind, user_id, lease_key, payload) values (p_kind, p_user, p_lease_key, p_payload) returning id;
$$;
revoke execute on function claim_jobs(int, int), complete_job(uuid, text), fail_job(uuid, text),
  enqueue_job(uuid, text, text, jsonb) from public, anon, authenticated;
```
pg_cron 등록 `0002_cron.sql`(URL·키는 `vault`에서 읽음. Supabase CLI는 `<숫자>_<이름>.sql`만 적용하므로 `0001b_` 같은 이름은 건너뛴다):
```sql
select cron.schedule('worker-every-minute', '* * * * *', $$
  select net.http_post(
    url := (select decrypted_secret from vault.decrypted_secrets where name='worker_url'),
    headers := jsonb_build_object('Authorization', 'Bearer ' || (select decrypted_secret from vault.decrypted_secrets where name='service_role_key'), 'Content-Type','application/json'),
    body := '{}'::jsonb, timeout_milliseconds := 5000);
$$);
```

- [ ] **Step 3: 마이그레이션 — items·user_keys·audit_log (봉투 암호화 저장소)**

`0003_items_keys.sql` (스펙 §8 정의를 따른다. 원문 평문 컬럼은 없다):
```sql
create table user_keys (
  user_id uuid primary key references auth.users on delete cascade,   -- 계정 삭제 = crypto-shredding
  wrapped_key bytea not null,             -- 사용자 데이터 키(32B)를 MASTER_KEY로 AES-256-GCM 감싼 값
  created_at timestamptz not null default now()
);
create table items (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users on delete cascade,
  source text not null check (source in ('GMAIL','MESSAGES','NOTIFICATION','SHARE','CHAT')),
  app_name text,
  sender text,
  title text,
  content_enc bytea,                      -- 원문. Edge에서 사용자 데이터 키로 암호화. 원문 만료 시 null
  ocr_text_enc bytea,                     -- 이미지 OCR 원문. 같은 방식
  occurred_at timestamptz not null,
  captured_at timestamptz not null default now(),
  device_filter text,
  idempotency_key text not null,
  status text not null default 'queued',
  storage_key text,
  expires_at timestamptz not null default now() + interval '90 days',
  unique (user_id, idempotency_key)
);
create table audit_log (
  id bigint generated always as identity primary key,
  user_id uuid not null,                  -- FK 없음: 계정 삭제 후에도 사유 코드 행은 남긴다(§8)
  actor text not null,
  action text not null,
  target text,
  at timestamptz not null default now()
);
alter table user_keys enable row level security;
alter table items enable row level security;
alter table audit_log enable row level security;
create policy user_keys_owner_read on user_keys for select using ((select auth.uid()) = user_id);
create policy items_owner_read on items for select using ((select auth.uid()) = user_id);
create policy audit_owner_read on audit_log for select using ((select auth.uid()) = user_id);

create or replace function get_wrapped_key(p_user uuid) returns bytea language sql stable as $$
  select wrapped_key from user_keys where user_id = p_user;
$$;
-- 동시에 두 함수가 키를 만들면 먼저 저장된 값을 돌려준다(둘 다 같은 키로 암호화하게 됨)
create or replace function put_wrapped_key(p_user uuid, p_wrapped bytea) returns bytea language plpgsql as $$
begin
  insert into user_keys (user_id, wrapped_key) values (p_user, p_wrapped) on conflict (user_id) do nothing;
  return (select wrapped_key from user_keys where user_id = p_user);
end $$;

create or replace function insert_item(p_user uuid, p_source text, p_idempotency_key text, p_sender text, p_title text,
                                       p_content_enc bytea, p_occurred_at timestamptz, p_enqueue boolean default true,
                                       p_app_name text default null, p_ocr_text_enc bytea default null)
returns uuid language plpgsql as $$
declare v_id uuid;
begin
  insert into items (user_id, source, app_name, sender, title, content_enc, ocr_text_enc, occurred_at, idempotency_key)
  values (p_user, p_source, p_app_name, p_sender, p_title, p_content_enc, p_ocr_text_enc, p_occurred_at, p_idempotency_key)
  on conflict (user_id, idempotency_key) do nothing
  returning id into v_id;
  if v_id is not null and p_enqueue then
    insert into jobs (kind, user_id, lease_key, payload)
    values ('process', p_user, 'item:' || v_id, jsonb_build_object('item_id', v_id));
  end if;
  return v_id;                            -- 중복이면 null
end $$;

-- §12 통제 4(b): items.user_id와 user_keys 소유자가 p_user로 같을 때만 암호문을 돌려주고 복호화 감사를 남긴다
create or replace function worker_get_item(p_user uuid, p_item uuid)
returns table (id uuid, content_enc bytea, ocr_text_enc bytea) language plpgsql as $$
#variable_conflict use_column
begin
  return query
    select i.id, i.content_enc, i.ocr_text_enc from items i join user_keys k on k.user_id = i.user_id
    where i.id = p_item and i.user_id = p_user and i.content_enc is not null;
  if found then
    insert into audit_log (user_id, actor, action, target) values (p_user, 'worker', 'decrypt', p_item::text);
  end if;
end $$;

revoke execute on function get_wrapped_key(uuid), put_wrapped_key(uuid, bytea),
  insert_item(uuid, text, text, text, text, bytea, timestamptz, boolean, text, bytea), worker_get_item(uuid, uuid)
  from public, anon, authenticated;
```

- [ ] **Step 4: jobs 실패 테스트 (deno, 호스팅 DB 대상)**

`tests/jobs.test.ts`:
```ts
import { assertEquals } from "jsr:@std/assert";
import { createClient } from "npm:@supabase/supabase-js@2";
import { encrypt, toBytea } from "../functions/_shared/crypto.ts";
const sb = createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);
const USER = Deno.env.get("POC_USER_ID")!;

Deno.test("same lease_key never runs twice concurrently", async () => {
  await sb.from("jobs").delete().neq("kind", "");
  await sb.from("jobs").insert([{ kind: "t", lease_key: "u1" }, { kind: "t", lease_key: "u1" }]);
  const a = await sb.rpc("claim_jobs", { p_limit: 10, p_lease_seconds: 60 });
  assertEquals(a.error, null);
  assertEquals(a.data!.length, 1);
  const b = await sb.rpc("claim_jobs", { p_limit: 10, p_lease_seconds: 60 });
  assertEquals(b.data!.length, 0);
});

Deno.test("expired lease is reclaimable and dead after 5 attempts", async () => {
  await sb.from("jobs").delete().neq("kind", "");
  const { data } = await sb.from("jobs").insert({ kind: "t", lease_key: "u2" }).select().single();
  for (let i = 0; i < 5; i++) {
    const c = await sb.rpc("claim_jobs", { p_limit: 1, p_lease_seconds: 0 });
    assertEquals(c.data!.length, 1);
    await sb.rpc("fail_job", { p_id: data!.id, p_error: "boom" });
  }
  const { data: j } = await sb.from("jobs").select().eq("id", data!.id).single();
  assertEquals(j!.status, "dead");
});

Deno.test("insert_item stores ciphertext, enqueues process job, dedups by idempotency_key", async () => {
  await sb.from("jobs").delete().neq("kind", "");
  const key = "test:" + crypto.randomUUID();
  const args = { p_user: USER, p_source: "SHARE", p_idempotency_key: key, p_sender: null, p_title: "합성",
    p_content_enc: toBytea(await encrypt(USER, "합성 문구: 10월 2일 오후 3시 치과 예약")), p_occurred_at: new Date().toISOString() };
  const first = await sb.rpc("insert_item", args);
  const again = await sb.rpc("insert_item", args);
  assertEquals(typeof first.data, "string");
  assertEquals(again.data, null);
  const { data: jobs } = await sb.from("jobs").select("kind, user_id, payload").eq("kind", "process");
  assertEquals(jobs!.length, 1);
  assertEquals(jobs![0].payload.item_id, first.data);
  await sb.from("items").delete().eq("id", first.data);
});

Deno.test("worker_get_item returns nothing for a mismatched user and audits only real reads", async () => {
  const { data: id } = await sb.rpc("insert_item", { p_user: USER, p_source: "SHARE", p_idempotency_key: "test:" + crypto.randomUUID(),
    p_sender: null, p_title: null, p_content_enc: toBytea(await encrypt(USER, "합성")), p_occurred_at: new Date().toISOString(), p_enqueue: false });
  const other = await sb.rpc("worker_get_item", { p_user: crypto.randomUUID(), p_item: id });
  assertEquals(other.data!.length, 0);
  const mine = await sb.rpc("worker_get_item", { p_user: USER, p_item: id });
  assertEquals(mine.data!.length, 1);
  const { count } = await sb.from("audit_log").select("*", { count: "exact", head: true }).eq("target", id).eq("action", "decrypt");
  assertEquals(count, 1);
  await sb.from("items").delete().eq("id", id);
});
```
실행: `cd poc/server && deno test --allow-net --allow-env --allow-read --env-file=.env supabase/tests/jobs.test.ts` → 마이그레이션 전이면 실패. 테스트 중에는 워커 cron이 잡을 가져가지 않도록 `select cron.alter_job((select jobid from cron.job where jobname = 'worker-every-minute'), active := false);`로 멈추고 끝나면 `active := true`로 되돌린다. `supabase db push` 후 Step 5의 crypto.ts까지 만들고 다시 돌려 통과.

- [ ] **Step 5: 봉투 암호화 모듈 테스트 → 구현**

`tests/crypto.test.ts` (네트워크·비밀값 없이 메모리 키 저장소로 실행):
```ts
import { assertEquals, assertNotEquals, assertRejects } from "jsr:@std/assert";
import { createEnvelope, fromBytea, type KeyStore, toBytea } from "../functions/_shared/crypto.ts";

function memStore(): KeyStore & { rows: Map<string, Uint8Array> } {
  const rows = new Map<string, Uint8Array>();
  return {
    rows,
    get: async (u) => rows.get(u) ?? null,
    putIfAbsent: async (u, w) => { if (!rows.has(u)) rows.set(u, w); return rows.get(u)!; },
  };
}
const randomKey = () => btoa(String.fromCharCode(...crypto.getRandomValues(new Uint8Array(32))));
const MASTER = randomKey();
const A = "00000000-0000-4000-8000-00000000000a";
const B = "00000000-0000-4000-8000-00000000000b";
const TEXT = "합성 문구: 10월 2일 오후 3시 강남 치과 예약 확인";

Deno.test("round trip through bytea hex", async () => {
  const env = createEnvelope(memStore(), MASTER);
  const ct = await env.encrypt(A, TEXT);
  assertEquals(await env.decrypt(A, toBytea(ct)), TEXT);
  assertEquals(fromBytea(toBytea(ct)), ct);
});

Deno.test("same plaintext encrypts differently (random IV) and one key per user", async () => {
  const store = memStore();
  const env = createEnvelope(store, MASTER);
  assertNotEquals(toBytea(await env.encrypt(A, TEXT)), toBytea(await env.encrypt(A, TEXT)));
  assertEquals(store.rows.size, 1);
});

Deno.test("another user's key cannot decrypt", async () => {
  const env = createEnvelope(memStore(), MASTER);
  const ct = await env.encrypt(A, TEXT);
  await env.encrypt(B, "B 키 생성용 합성 문구");
  await assertRejects(() => env.decrypt(B, ct));
});

Deno.test("wrapped key copied to another user's row does not unwrap", async () => {
  const store = memStore();
  const ct = await createEnvelope(store, MASTER).encrypt(A, TEXT);
  store.rows.set(B, store.rows.get(A)!);
  await assertRejects(() => createEnvelope(store, MASTER).decrypt(B, ct));
});

Deno.test("wrong master key or deleted user_keys row cannot decrypt (crypto-shredding)", async () => {
  const store = memStore();
  const ct = await createEnvelope(store, MASTER).encrypt(A, TEXT);
  await assertRejects(() => createEnvelope(store, randomKey()).decrypt(A, ct));
  store.rows.delete(A);
  await assertRejects(() => createEnvelope(store, MASTER).decrypt(A, ct), Error, "no data key");
  assertEquals(store.rows.size, 0);       // 복호화 경로는 키를 새로 만들지 않는다
});
```
실행: `deno test supabase/tests/crypto.test.ts` → 모듈이 없어 실패.

`functions/_shared/crypto.ts` (WebCrypto만 사용):
```ts
import { createClient, type SupabaseClient } from "npm:@supabase/supabase-js@2";

// 봉투 암호화 (스펙 §12 통제 1)
// wrapped_key = v1 || iv(12) || AES-256-GCM(MASTER_KEY, DEK, aad="user_keys:<userId>")
// content_enc = v1 || iv(12) || AES-256-GCM(DEK, plaintext, aad="items:<userId>")
export interface KeyStore {
  get(userId: string): Promise<Uint8Array | null>;
  putIfAbsent(userId: string, wrapped: Uint8Array): Promise<Uint8Array>;   // 경합 시 먼저 저장된 값을 반환
}

const VERSION = 1;
// WebCrypto 호출에는 .slice()로 ArrayBuffer 기반 복사본을 넘긴다(Deno 타입 검사: BufferSource)
const te = new TextEncoder(), td = new TextDecoder();

export function toBytea(b: Uint8Array): string {
  return "\\x" + Array.from(b, (x) => x.toString(16).padStart(2, "0")).join("");
}
export function fromBytea(v: Uint8Array | string): Uint8Array {
  if (typeof v !== "string") return v;
  const hex = v.startsWith("\\x") ? v.slice(2) : v;
  if (hex.length % 2 !== 0 || /[^0-9a-f]/i.test(hex)) throw new Error("bytea: invalid hex");
  const out = new Uint8Array(hex.length / 2);
  for (let i = 0; i < out.length; i++) out[i] = parseInt(hex.slice(i * 2, i * 2 + 2), 16);
  return out;
}

async function aesKey(raw: Uint8Array): Promise<CryptoKey> {
  if (raw.length !== 32) throw new Error("key must be 32 bytes");
  return await crypto.subtle.importKey("raw", raw.slice(), { name: "AES-GCM" }, false, ["encrypt", "decrypt"]);
}
async function seal(key: CryptoKey, plain: Uint8Array, aad: Uint8Array): Promise<Uint8Array> {
  const iv = crypto.getRandomValues(new Uint8Array(12));
  const ct = new Uint8Array(await crypto.subtle.encrypt({ name: "AES-GCM", iv, additionalData: aad.slice() }, key, plain.slice()));
  const out = new Uint8Array(13 + ct.length);
  out[0] = VERSION; out.set(iv, 1); out.set(ct, 13);
  return out;
}
async function open(key: CryptoKey, blob: Uint8Array, aad: Uint8Array): Promise<Uint8Array> {
  if (blob.length < 13 + 16 || blob[0] !== VERSION) throw new Error("ciphertext: bad format");
  const pt = await crypto.subtle.decrypt({ name: "AES-GCM", iv: blob.slice(1, 13), additionalData: aad.slice() }, key, blob.slice(13));
  return new Uint8Array(pt);
}

export function createEnvelope(store: KeyStore, masterKeyB64: string) {
  const master = aesKey(Uint8Array.from(atob(masterKeyB64), (c) => c.charCodeAt(0)));
  const cache = new Map<string, Promise<CryptoKey>>();   // 한 번의 함수 호출 안에서 unwrap 1회

  async function load(userId: string, create: boolean): Promise<CryptoKey> {
    const aad = te.encode("user_keys:" + userId);
    let wrapped = await store.get(userId);
    if (!wrapped) {
      if (!create) throw new Error("no data key for user");
      const dek = crypto.getRandomValues(new Uint8Array(32));
      wrapped = await store.putIfAbsent(userId, await seal(await master, dek, aad));
    }
    return await aesKey(await open(await master, wrapped, aad));
  }
  function dataKey(userId: string, create: boolean): Promise<CryptoKey> {
    let p = cache.get(userId);
    if (!p) {
      p = load(userId, create);
      cache.set(userId, p);
      p.catch(() => cache.delete(userId));
    }
    return p;
  }

  return {
    async encrypt(userId: string, plaintext: string): Promise<Uint8Array> {
      return await seal(await dataKey(userId, true), te.encode(plaintext), te.encode("items:" + userId));
    },
    async decrypt(userId: string, data: Uint8Array | string): Promise<string> {
      return td.decode(await open(await dataKey(userId, false), fromBytea(data), te.encode("items:" + userId)));
    },
  };
}

// 오류에는 PostgREST 코드만 담는다(메시지에 값이 섞이지 않게)
export function supabaseKeyStore(sb: SupabaseClient): KeyStore {
  return {
    async get(userId) {
      const { data, error } = await sb.rpc("get_wrapped_key", { p_user: userId });
      if (error) throw new Error("get_wrapped_key " + error.code);
      return data ? fromBytea(data as string) : null;
    },
    async putIfAbsent(userId, wrapped) {
      const { data, error } = await sb.rpc("put_wrapped_key", { p_user: userId, p_wrapped: toBytea(wrapped) });
      if (error) throw new Error("put_wrapped_key " + error.code);
      return fromBytea(data as string);
    },
  };
}

let envelope: ReturnType<typeof createEnvelope> | null = null;
function defaultEnvelope() {
  envelope ??= createEnvelope(
    supabaseKeyStore(createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!)),
    Deno.env.get("MASTER_KEY")!,
  );
  return envelope;
}
export const encrypt = (userId: string, plaintext: string) => defaultEnvelope().encrypt(userId, plaintext);
export const decrypt = (userId: string, data: Uint8Array | string) => defaultEnvelope().decrypt(userId, data);
```
실행: `deno test supabase/tests/crypto.test.ts` → 5개 통과. 이어서 Step 4의 `jobs.test.ts` → 4개 통과.

- [ ] **Step 6: 서버 규칙 필터 테스트 → 구현 (스펙 §7 "서버 규칙 필터 재적용")**

기기 `RuleFilter`(Task 2)와 같은 정규식·케이스를 쓴다. **정규식은 원안이 아니라 `0d2a293` 이후 `RuleFilter.swift`(스펙 §6 "기기 규칙 필터")를 옮기고, `RuleFilterTests`의 회귀 케이스(날짜 뒤 카드, 하이픈 계좌, 승인번호 예외, Hotpot 등)도 함께 옮긴다.** 차이는 두 가지다: 연락처 발신자 규칙은 연락처가 기기에만 있어 서버에서 빼고, 프로모션(Gmail `CATEGORY_PROMOTIONS` 라벨, 본문·제목·발신자 앞의 `(광고)`/`[광고]` 표기)을 폐기 사유로 더한다. 기기의 프로모션 판정은 Foundation Models(Task 5)가 맡으므로 서버 규칙이 그 대체 역할을 한다.

`tests/rules.test.ts`:
```ts
import { assertEquals } from "jsr:@std/assert";
import { applyRules, luhn, maskSensitive } from "../functions/_shared/rules.ts";

// Task 2 RuleFilterTests와 같은 케이스 (연락처 규칙 제외)
Deno.test("OTP (Korean) discarded", () => {
  assertEquals(applyRules("[Web발신] 인증번호 483920 을 입력하세요"), { kind: "discard", reason: "otp" });
});
Deno.test("verification code (English) discarded", () => {
  assertEquals(applyRules("Your verification code is 1234"), { kind: "discard", reason: "otp" });
});
Deno.test("Luhn-valid card masked except last 4", () => {
  assertEquals(applyRules("카드 4532-0151-1283-0366 승인 32,000원"), { kind: "pass", masked: "카드 ****-****-****-0366 승인 32,000원" });
});
Deno.test("non-Luhn number kept", () => {
  assertEquals(applyRules("송장번호 1234567890123"), { kind: "pass", masked: "송장번호 1234567890123" });
});
Deno.test("account number masked", () => {
  assertEquals(applyRules("국민은행 계좌 12345678901234 로 입금"), { kind: "pass", masked: "국민은행 계좌 **********1234 로 입금" });
});
Deno.test("plain notice passes unchanged", () => {
  assertEquals(applyRules("내일 오후 3시 진료 예약입니다", { sender: "서울병원" }), { kind: "pass", masked: "내일 오후 3시 진료 예약입니다" });
});
// 서버 전용 케이스
Deno.test("contact-name sender is not filtered on server (device-only rule)", () => {
  assertEquals(applyRules("토요일 2시에 보자", { sender: "김민수" }), { kind: "pass", masked: "토요일 2시에 보자" });
});
Deno.test("OTP in title discarded", () => {
  assertEquals(applyRules("아래 번호를 입력하세요", { title: "인증번호 [771203]" }), { kind: "discard", reason: "otp" });
});
Deno.test("promotion: Gmail label, (광고) in text/title/sender", () => {
  const promo = { kind: "discard", reason: "promotion" } as const;
  assertEquals(applyRules("가을 세일", { labels: ["INBOX", "CATEGORY_PROMOTIONS"] }), promo);
  assertEquals(applyRules("[Web발신](광고) 가을 세일 최대 50%"), promo);
  assertEquals(applyRules("세일 안내", { title: "(광고) 가을 세일" }), promo);
  assertEquals(applyRules("세일 안내", { sender: "[광고] 합성쇼핑" }), promo);
  assertEquals(applyRules("광고 문의는 고객센터로", { title: "주문 확인" }).kind, "pass");
});
Deno.test("maskSensitive for titles and luhn helper", () => {
  assertEquals(maskSensitive("결제 완료 4532015112830366"), "결제 완료 ************0366");
  assertEquals(luhn("4532015112830366"), true);
  assertEquals(luhn("1234567890123"), false);
});
```
실행: `deno test supabase/tests/rules.test.ts` → 모듈이 없어 실패.

`functions/_shared/rules.ts`:
```ts
// 서버 규칙 필터. 기기 RuleFilter(Task 2)·스펙 §6과 같은 정규식·순서를 쓴다.
// 연락처 발신자 규칙은 연락처가 기기에만 있어 서버에서는 적용하지 않는다(§7).
export type RuleVerdict = { kind: "discard"; reason: "otp" | "promotion" } | { kind: "pass"; masked: string };
export type RuleMeta = { sender?: string | null; title?: string | null; labels?: string[] };

const OTP_KEYWORD = /(인증|승인|확인)\s*번호|verification|OTP/i;
const OTP_DIGITS = /(?<!\d)\d{4,8}(?!\d)/;
const CARD_LIKE = /(?<!\d)(?:\d[ -]?){12,18}\d(?!\d)/g;
const ACCOUNT = /(은행|계좌).{0,20}?(?<!\d)(\d{10,14})(?!\d)/g;
const AD_MARK = /^\s*(?:\[Web발신\]\s*)?[(\[]\s*광고\s*[)\]]/;   // 정보통신망법 광고 표기 "(광고)"

export function luhn(digits: string): boolean {
  let sum = 0, alt = false;
  for (let i = digits.length - 1; i >= 0; i--) {
    let d = digits.charCodeAt(i) - 48;
    if (d < 0 || d > 9) return false;
    if (alt) { d *= 2; if (d > 9) d -= 9; }
    sum += d; alt = !alt;
  }
  return sum % 10 === 0;
}

export function isOtp(s: string): boolean {
  return OTP_KEYWORD.test(s) && OTP_DIGITS.test(s);
}

// 카드번호(13~19자리, Luhn 통과)는 마지막 4자리만, 계좌번호(은행·계좌 키워드 뒤 20자 이내 10~14자리)도 마지막 4자리만 남긴다
export function maskSensitive(s: string): string {
  const carded = s.replace(CARD_LIKE, (raw) => {
    const digits = raw.replace(/\D/g, "");
    if (digits.length < 13 || digits.length > 19 || !luhn(digits)) return raw;
    let seen = 0;
    return raw.replace(/\d/g, (d) => (++seen > digits.length - 4 ? d : "*"));
  });
  return carded.replace(ACCOUNT, (m: string, _kw: string, num: string) =>
    m.slice(0, m.length - num.length) + "*".repeat(num.length - 4) + num.slice(-4));
}

export function applyRules(text: string, meta: RuleMeta = {}): RuleVerdict {
  if (isOtp(text) || isOtp(meta.title ?? "")) return { kind: "discard", reason: "otp" };
  if (meta.labels?.includes("CATEGORY_PROMOTIONS")) return { kind: "discard", reason: "promotion" };
  if ([text, meta.title ?? "", meta.sender ?? ""].some((s) => AD_MARK.test(s))) return { kind: "discard", reason: "promotion" };
  return { kind: "pass", masked: maskSensitive(text) };
}
```
실행: `deno test supabase/tests/rules.test.ts` → 10개 통과.

- [ ] **Step 7: ingest Edge Function 테스트 → 구현 (JWT → 규칙 → 암호화 → insert_item + process 잡)**

핸들러를 의존성 주입형으로 두어 네트워크 없이 테스트한다. `idempotency_key`는 `<source>:<기기 CaptureItem.id>`(스펙 §7의 `source + external_id`). 기기 `JSONEncoder` 기본 날짜(2001-01-01 기준 초)와 ISO 문자열을 둘 다 받는다. 이미지·PDF 파일(`PUT /upload/<id>`)은 이 함수가 받지 않는다(Task 12 Storage 경로).

`tests/ingest.test.ts`:
```ts
import { assertEquals } from "jsr:@std/assert";
import { handleIngest, type IngestDeps, type NewItem, parseCapturedAt } from "../functions/ingest/handler.ts";

// 가짜 의존성: encrypt는 평문 바이트를 그대로 돌려줘 무엇이 암호화됐는지 검사한다
function deps(o: { user?: string | null; duplicate?: boolean } = {}) {
  const inserted: NewItem[] = [];
  const d: IngestDeps = {
    authUser: async (t) => (t === "good" ? (o.user === undefined ? "user-1" : o.user) : null),
    encrypt: async (_u, p) => new TextEncoder().encode(p),
    insertItem: async (item) => { inserted.push(item); return o.duplicate ? null : "item-1"; },
  };
  return { d, inserted };
}
const req = (body: unknown, token: string | null = "good") => new Request("http://x/ingest", {
  method: "POST", body: JSON.stringify(body),
  headers: { "content-type": "application/json", ...(token ? { authorization: `Bearer ${token}` } : {}) },
});
const base = { id: "A1B2", source: "NOTIFICATION", appName: "KakaoTalk", sender: null, title: "합성쇼핑",
  text: "주문이 접수되었습니다", ocrText: null, capturedAt: 780000000, attempts: 0 };
const dec = (b: Uint8Array | null) => (b ? new TextDecoder().decode(b) : null);

Deno.test("401 without or with invalid token, nothing stored", async () => {
  const { d, inserted } = deps();
  assertEquals((await handleIngest(req(base, null), d)).status, 401);
  assertEquals((await handleIngest(req(base, "bad"), d)).status, 401);
  assertEquals(inserted.length, 0);
});

Deno.test("OTP text or OTP in OCR → 204, nothing stored", async () => {
  const { d, inserted } = deps();
  assertEquals((await handleIngest(req({ ...base, text: "[Web발신] 인증번호 483920 을 입력하세요" }), d)).status, 204);
  assertEquals((await handleIngest(req({ ...base, source: "SHARE", text: "사진", ocrText: "승인번호 5521 입력" }), d)).status, 204);
  assertEquals(inserted.length, 0);
});

Deno.test("promotion (광고) → 204", async () => {
  const { d, inserted } = deps();
  assertEquals((await handleIngest(req({ ...base, text: "(광고) 가을 세일 최대 50%" }), d)).status, 204);
  assertEquals(inserted.length, 0);
});

Deno.test("card/account masked before encrypt, title masked, idempotency key = source:id", async () => {
  const { d, inserted } = deps();
  const r = await handleIngest(req({ ...base, source: "MESSAGES", title: "결제 4532015112830366",
    text: "카드 4532-0151-1283-0366 승인 32,000원", ocrText: "국민은행 계좌 12345678901234 로 입금" }), d);
  assertEquals(r.status, 202);
  assertEquals(await r.json(), { item_id: "item-1", duplicate: false });
  const it = inserted[0];
  assertEquals(dec(it.contentEnc), "카드 ****-****-****-0366 승인 32,000원");
  assertEquals(dec(it.ocrTextEnc), "국민은행 계좌 **********1234 로 입금");
  assertEquals(it.title, "결제 ************0366");
  assertEquals(it.idempotencyKey, "MESSAGES:A1B2");
  assertEquals(it.user, "user-1");
});

Deno.test("duplicate → 202 with duplicate flag", async () => {
  const { d } = deps({ duplicate: true });
  const r = await handleIngest(req(base), d);
  assertEquals(r.status, 202);
  assertEquals((await r.json()).duplicate, true);
});

Deno.test("bad body → 400 (unknown source, GMAIL from device, missing text, bad date)", async () => {
  const { d } = deps();
  for (const b of [{ ...base, source: "X" }, { ...base, source: "GMAIL" }, { ...base, text: undefined }, { ...base, capturedAt: "nope" }]) {
    assertEquals((await handleIngest(req(b), d)).status, 400);
  }
});

Deno.test("capturedAt: Swift reference-date seconds and ISO strings", () => {
  assertEquals(parseCapturedAt(0), "2001-01-01T00:00:00.000Z");
  assertEquals(parseCapturedAt("2026-09-23T09:00:00+09:00"), "2026-09-23T00:00:00.000Z");
});
```
실행: `deno test supabase/tests/ingest.test.ts` → 모듈이 없어 실패.

`functions/ingest/handler.ts`:
```ts
import { applyRules, maskSensitive } from "../_shared/rules.ts";

// 기기 CaptureItem(Task 3) JSON. localFile 항목은 이 경로로 오지 않는다(파일은 Storage 경로)
export type IngestBody = {
  id: string; source: string; appName?: string | null; sender?: string | null; title?: string | null;
  text: string; ocrText?: string | null; capturedAt: number | string;
};
export type NewItem = {
  user: string; source: string; idempotencyKey: string; appName: string | null; sender: string | null;
  title: string | null; contentEnc: Uint8Array; ocrTextEnc: Uint8Array | null; occurredAt: string;
};
export type IngestDeps = {
  authUser(token: string): Promise<string | null>;                       // JWT → user_id, 실패 시 null
  encrypt(userId: string, plaintext: string): Promise<Uint8Array>;
  insertItem(item: NewItem): Promise<string | null>;                      // 중복이면 null
};

const SOURCES = new Set(["MESSAGES", "NOTIFICATION", "SHARE", "CHAT"]);   // GMAIL은 서버가 직접 수집
const APPLE_EPOCH = 978307200;                                           // Swift JSONEncoder 기본 Date = 2001-01-01 기준 초

export function parseCapturedAt(v: unknown): string | null {
  const ms = typeof v === "number" ? (v + APPLE_EPOCH) * 1000 : typeof v === "string" ? Date.parse(v) : NaN;
  return Number.isFinite(ms) ? new Date(ms).toISOString() : null;
}

function discarded(reason: string) {
  console.log(JSON.stringify({ ingest: "discard", reason }));             // 사유 코드만, 본문 금지
  return new Response(null, { status: 204 });
}

// 스펙 §7 /ingest: JWT → 서버 규칙 필터 → 암호화 → items INSERT + jobs(process) → 202
export async function handleIngest(req: Request, deps: IngestDeps): Promise<Response> {
  const token = req.headers.get("authorization")?.match(/^Bearer\s+(.+)$/i)?.[1];
  const user = token ? await deps.authUser(token) : null;
  if (!user) return new Response(null, { status: 401 });
  let b: IngestBody;
  try { b = await req.json(); } catch { return new Response(null, { status: 400 }); }
  const occurredAt = parseCapturedAt(b?.capturedAt);
  if (typeof b?.id !== "string" || typeof b.text !== "string" || !SOURCES.has(b.source) || !occurredAt) {
    return new Response(null, { status: 400 });
  }
  const v = applyRules(b.text, { sender: b.sender, title: b.title });
  if (v.kind === "discard") return discarded(v.reason);
  let ocr: string | null = null;
  if (b.ocrText) {
    const o = applyRules(b.ocrText);                                      // 기기에서 적용된 OCR에도 재적용(§12 통제 2)
    if (o.kind === "discard") return discarded(o.reason);
    ocr = o.masked;
  }
  const itemId = await deps.insertItem({
    user, source: b.source, idempotencyKey: `${b.source}:${b.id}`,
    appName: b.appName ?? null, sender: b.sender ?? null, title: b.title ? maskSensitive(b.title) : null,
    contentEnc: await deps.encrypt(user, v.masked),
    ocrTextEnc: ocr === null ? null : await deps.encrypt(user, ocr),
    occurredAt,
  });
  return Response.json({ item_id: itemId, duplicate: itemId === null }, { status: 202 });
}
```

`functions/ingest/index.ts`:
```ts
import { createClient } from "npm:@supabase/supabase-js@2";
import { encrypt, toBytea } from "../_shared/crypto.ts";
import { handleIngest } from "./handler.ts";
const sb = createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);
Deno.serve((req) => handleIngest(req, {
  authUser: async (token) => {
    const { data, error } = await sb.auth.getUser(token);
    return error ? null : data.user?.id ?? null;
  },
  encrypt,
  insertItem: async (a) => {
    const { data, error } = await sb.rpc("insert_item", {
      p_user: a.user, p_source: a.source, p_idempotency_key: a.idempotencyKey, p_sender: a.sender, p_title: a.title,
      p_content_enc: toBytea(a.contentEnc), p_occurred_at: a.occurredAt,
      p_app_name: a.appName, p_ocr_text_enc: a.ocrTextEnc ? toBytea(a.ocrTextEnc) : null,
    });
    if (error) throw new Error("insert_item " + error.code);
    return data as string | null;
  },
}));
```
실행: `deno test supabase/tests/ingest.test.ts` → 7개 통과. 배포: `supabase functions deploy ingest`(JWT 검증 유지. 게이트웨이 검증에 더해 `auth.getUser`로 user_id를 얻는다).

- [ ] **Step 8: worker Edge Function (process 단계는 복호화를 거친다)**

`functions/_shared/job.ts`:
```ts
export type Job = {
  id: string;
  kind: string;
  user_id: string | null;
  payload: Record<string, unknown>;
  attempts: number;
  checkpoint: string | null;
};
```

`functions/worker/process.ts` — 분류·추출처럼 원문이 필요한 단계는 이 함수를 통해서만 평문을 받는다:
```ts
import type { SupabaseClient } from "npm:@supabase/supabase-js@2";
import { decrypt } from "../_shared/crypto.ts";
import type { Job } from "../_shared/job.ts";

// 평문을 받는 다음 단계. 반환값이 체크포인트가 된다
export type Extract = (userId: string, itemId: string, text: string) => Promise<string>;

export async function processItem(sb: SupabaseClient, job: Job, extract: Extract): Promise<string> {
  if (!job.user_id) throw new Error("process job without user_id");
  const itemId = String(job.payload.item_id);
  const { data, error } = await sb.rpc("worker_get_item", { p_user: job.user_id, p_item: itemId });
  if (error) throw new Error("worker_get_item " + error.code);
  const row = (data as { content_enc: string }[])[0];
  if (!row) throw new Error("worker_get_item not_found");
  const t0 = performance.now();
  const text = await decrypt(job.user_id, row.content_enc);
  const decryptMs = performance.now() - t0;
  const checkpoint = await extract(job.user_id, itemId, text);
  console.log(JSON.stringify({ item_id: itemId, decrypt_ms: Math.round(decryptMs * 10) / 10, chars: text.length }));  // 본문 금지
  return checkpoint;
}
```

`functions/worker/index.ts`:
```ts
import { createClient } from "npm:@supabase/supabase-js@2";
import type { Job } from "../_shared/job.ts";
import { processItem } from "./process.ts";
const sb = createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);
const handlers: Record<string, (job: Job) => Promise<string>> = {
  noop: async () => "done",
  sleep: async (j) => { await new Promise((r) => setTimeout(r, Number(j.payload.ms ?? 0))); return "done"; },
  // PoC-10에서는 복호화 비용만 측정한다. Task 12 Step 3에서 extract를 extractEvent로 교체한다
  process: (j) => processItem(sb, j, async () => "decrypted"),
};
Deno.serve(async () => {
  const { data: jobs, error } = await sb.rpc("claim_jobs", { p_limit: 5, p_lease_seconds: 60 });
  if (error) return new Response(error.code, { status: 500 });
  const results = [];
  for (const j of (jobs ?? []) as Job[]) {
    try {
      const run = handlers[j.kind] ?? (async () => { throw new Error("unknown kind " + j.kind); });
      const cp = await run(j);
      await sb.rpc("complete_job", { p_id: j.id, p_checkpoint: cp });
      results.push([j.id, "done"]);
    } catch (e) {
      // 오류 메시지는 코드·식별자만 담도록 각 모듈이 만든다. 길이도 제한한다
      await sb.rpc("fail_job", { p_id: j.id, p_error: (e instanceof Error ? e.message : "error").slice(0, 200) });
      results.push([j.id, "fail"]);
    }
  }
  return Response.json({ claimed: jobs?.length ?? 0, results });
});
```
배포: `supabase functions deploy worker`(JWT 검증 유지, cron이 service role로 호출). vault에 `worker_url`, `service_role_key` 저장(대시보드 SQL: `select vault.create_secret('<url>','worker_url')`).

`scripts/seed-items.ts` (합성 항목 N개를 암호화해 넣고 process 잡을 만든다):
```ts
import { createClient } from "npm:@supabase/supabase-js@2";
import { encrypt, toBytea } from "../supabase/functions/_shared/crypto.ts";
const sb = createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);
const user = Deno.env.get("POC_USER_ID")!;
const n = Number(Deno.args[0] ?? 20);
for (let i = 0; i < n; i++) {
  const text = `합성 주문 확인 #${i}: 무선 이어폰 1개, 결제 39,000원, 배송 예정 10월 2일. `.repeat(50);   // 약 5KB
  const { error } = await sb.rpc("insert_item", { p_user: user, p_source: "SHARE", p_idempotency_key: `seed:${crypto.randomUUID()}`,
    p_sender: null, p_title: `seed ${i}`, p_content_enc: toBytea(await encrypt(user, text)), p_occurred_at: new Date().toISOString() });
  if (error) throw new Error("insert_item " + error.code);
}
console.log(`seeded ${n}`);
```

- [ ] **Step 9: 검증 시나리오 (PoC-10, ingest)**

1. `noop` 잡 20개 insert → 2분 후 전부 `done`.
2. `sleep` 잡(ms=90000, 임대 60초) 1개 → 워커 2회 호출 시 두 번째가 재클레임하는지(중복 실행) 확인. 중복이면 임대 시간을 처리 예상 시간보다 길게 잡는 규칙을 results.md에 기록.
3. `unknown` 잡 1개 → 5분 후 `dead`, attempts=5.
4. 복호화 비용(스펙 §16 "Edge 복호화 비용"): `deno run --allow-net --allow-env --env-file=.env scripts/seed-items.ts 50` → 워커 로그의 `decrypt_ms` p50·p95, 호출당 5건 처리 시간을 기록하고 CPU 2초 안에 드는 배치 크기(`claim_jobs`의 `p_limit`)를 정한다. `audit_log`에 `decrypt` 50행이 생겼는지 확인.
5. 변조 검출: 한 항목의 `content_enc` 마지막 바이트를 SQL로 바꾼 뒤(`update items set content_enc = overlay(content_enc placing '\x00'::bytea from length(content_enc)) where id = '<id>'`) 해당 process 잡이 5회 후 `dead`가 되고, `last_error`·함수 로그에 본문이 없는지 확인.
6. ingest 실제 호출: PoC 사용자 JWT를 받아 세 건을 보낸다.
   ```bash
   TOKEN=$(deno eval --env-file=.env 'import { createClient } from "npm:@supabase/supabase-js@2";
   const sb = createClient(Deno.env.get("SUPABASE_URL"), Deno.env.get("SUPABASE_ANON_KEY"));
   const { data } = await sb.auth.signInWithPassword({ email: "poc-user@example.com", password: Deno.env.get("POC_USER_PASSWORD") });
   console.log(data.session.access_token);')
   for T in '카드 4532-0151-1283-0366 승인 32,000원' '[Web발신] 인증번호 483920 을 입력하세요' '(광고) 가을 세일'; do
     curl -s -o /dev/null -w '%{http_code}\n' -X POST "$SUPABASE_URL/functions/v1/ingest" -H "Authorization: Bearer $TOKEN" \
       -H 'content-type: application/json' -d "{\"id\":\"$(uuidgen)\",\"source\":\"MESSAGES\",\"text\":\"$T\",\"capturedAt\":780000000}"
   done
   ```
   기대: `202`, `204`, `204`. `items`에는 1행만 생기고 `content_enc`만 채워졌는지(평문 컬럼 없음), process 잡 1건이 생겼는지 확인한다. 토큰 없이 호출하면 401.
7. 24시간 두고 프로젝트가 일시정지되지 않는지 확인(무료 티어 활동 유지).

- [ ] **Step 10: 커밋** `git commit -m "poc(server): supabase project, jobs queue, envelope encryption, server rule filter, ingest, worker"`

---

### Task 9: Edge Function → APNs HTTP/2 (PoC-4)

**Files:**
- Create: `functions/_shared/apns.ts`, `functions/apns-send/index.ts`, `tests/apns.test.ts`

**Interfaces:**
- Produces: `sendAPNs({ token, payload, topic, priority }): Promise<{ status: number; apnsId?: string; reason?: string }>`. JWT는 모듈 스코프에 캐시해 20~60분 사이 갱신.

- [ ] **Step 1: APNs 키 준비 (사용자, 1회)**

developer.apple.com → Keys → APNs 키 생성 → `.p8` 다운로드, Key ID 기록. `supabase secrets set APNS_KEY_ID=... APNS_TEAM_ID=6626BYCJG4 APNS_TOPIC=com.picpal.assistant.poc APNS_P8="$(cat AuthKey.p8)"`.

- [ ] **Step 2: JWT 단위 테스트 → 구현**

`tests/apns.test.ts`:
```ts
import { assert, assertEquals } from "jsr:@std/assert";
import { makeJWT, __resetJWTCache } from "../functions/_shared/apns.ts";
Deno.test("jwt header/claims", async () => {
  __resetJWTCache();
  const jwt = await makeJWT({ keyId: "ABC123DEFG", teamId: "6626BYCJG4", p8: Deno.env.get("APNS_P8")! });
  const [h, c] = jwt.split(".").slice(0, 2).map((s) => JSON.parse(atob(s.replace(/-/g, "+").replace(/_/g, "/"))));
  assertEquals(h.alg, "ES256"); assertEquals(h.kid, "ABC123DEFG"); assertEquals(c.iss, "6626BYCJG4"); assert(typeof c.iat === "number");
});
Deno.test("jwt cached within 20 minutes", async () => {
  __resetJWTCache();
  const a = await makeJWT({ keyId: "ABC123DEFG", teamId: "6626BYCJG4", p8: Deno.env.get("APNS_P8")! });
  const b = await makeJWT({ keyId: "ABC123DEFG", teamId: "6626BYCJG4", p8: Deno.env.get("APNS_P8")! });
  assertEquals(a, b);
});
```

`_shared/apns.ts`:
```ts
let cached: { jwt: string; at: number } | null = null;
export function __resetJWTCache() { cached = null; }
function b64url(b: ArrayBuffer | Uint8Array | string) {
  const bytes = typeof b === "string" ? new TextEncoder().encode(b) : new Uint8Array(b);
  return btoa(String.fromCharCode(...bytes)).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
}
export async function makeJWT(o: { keyId: string; teamId: string; p8: string }) {
  const now = Math.floor(Date.now() / 1000);
  if (cached && now - cached.at < 30 * 60) return cached.jwt;          // 20~60분 규칙: 30분마다 갱신
  const pem = o.p8.replace(/-----[A-Z ]+-----/g, "").replace(/\s+/g, "");
  const key = await crypto.subtle.importKey("pkcs8", Uint8Array.from(atob(pem), (c) => c.charCodeAt(0)),
    { name: "ECDSA", namedCurve: "P-256" }, false, ["sign"]);
  const head = b64url(JSON.stringify({ alg: "ES256", kid: o.keyId })), claims = b64url(JSON.stringify({ iss: o.teamId, iat: now }));
  const sig = await crypto.subtle.sign({ name: "ECDSA", hash: "SHA-256" }, key, new TextEncoder().encode(`${head}.${claims}`));
  cached = { jwt: `${head}.${claims}.${b64url(sig)}`, at: now };
  return cached.jwt;
}
export async function sendAPNs(o: { token: string; payload: unknown; topic: string; priority?: 5 | 10; sandbox?: boolean }) {
  const jwt = await makeJWT({ keyId: Deno.env.get("APNS_KEY_ID")!, teamId: Deno.env.get("APNS_TEAM_ID")!, p8: Deno.env.get("APNS_P8")! });
  const host = o.sandbox === false ? "api.push.apple.com" : "api.sandbox.push.apple.com";
  const r = await fetch(`https://${host}/3/device/${o.token}`, { method: "POST",
    headers: { authorization: `bearer ${jwt}`, "apns-topic": o.topic, "apns-priority": String(o.priority ?? 10), "apns-push-type": "alert" },
    body: JSON.stringify(o.payload) });
  const apnsId = r.headers.get("apns-id") ?? undefined;
  if (r.status === 200) return { status: 200, apnsId };
  return { status: r.status, apnsId, reason: (await r.json().catch(() => ({}))).reason };
}
```

- [ ] **Step 3: apns-send Edge Function과 부하 시나리오**

`functions/apns-send/index.ts`: body `{ token, count, concurrency }` → `count`회 `sendAPNs`를 `concurrency` 동시로 실행, `{ ok, byStatus, reasons, h2Errors }` 반환. h2 오류는 `fetch`가 throw한 메시지에 `http2`/`stream` 포함 여부로 센다.

- [ ] **Step 4: 실기기 토큰 확보 후 100회 발송**

실기기에 AssistantPoC 설치 → `UIApplication.shared.registerForRemoteNotifications()` → `didRegisterForRemoteNotificationsWithDeviceToken`에서 hex 토큰을 poc.log에 기록(앱에 등록 버튼 추가). 그 토큰으로:
```bash
curl -X POST "$SUPABASE_URL/functions/v1/apns-send" -H "Authorization: Bearer $SERVICE_ROLE" -d '{"token":"<hex>","count":100,"concurrency":10}'
```
성공률·h2Errors를 results.md PoC-4에 기록. 실패율 >1% 또는 h2 오류 발생 시 대안(Cloudflare Worker 릴레이)을 채택하고 스펙 §3·§16을 갱신한다. 시뮬레이터로는 발송 성공(APNs 200)까지만 확인 불가하므로 이 태스크는 **실기기 1회 필요**.

- [ ] **Step 5: 커밋** `git commit -m "poc(server): APNs token auth + load test function"`

---

### Task 10: Gmail 연결·watch·history 동기화 (PoC-6)

**Files:**
- Create: `migrations/0005_gmail.sql`(connections·sync_states·Gmail 저장 프로시저·cron. `items`는 Task 8의 `0003_items_keys.sql`), `functions/_shared/gmail.ts`, `functions/_shared/gmail-jobs.ts`, `functions/gmail-connect/index.ts`, `functions/gmail-webhook/index.ts`, `tests/gmail.test.ts`, `poc/ios/App/GoogleSignIn.swift`
- Modify: `functions/worker/index.ts`(핸들러 3개 등록)

**Interfaces:**
- Consumes: Task 8의 `items`·`insert_item`·`enqueue_job`·`encrypt`·`toBytea`·`applyRules`·`maskSensitive`·`Job`·worker `handlers`.
- Produces:
  - `connections(id, user_id, provider, account_ref, status, expires_at)`, `sync_states(connection_id pk, user_id, cursor, last_success_at, watch_expires_at)`. refresh token은 `vault`에 `gmail_rt:<connection_id>` 이름으로 저장(스펙 §7). 만료 두 가지를 분리한다(스펙 §7·§8): `connections.expires_at` = OAuth refresh token 만료(테스트 모드 발급 후 7일), `sync_states.watch_expires_at` = Gmail watch 만료(7일, 매일 갱신).
  - 저장 프로시저: `gmail_save_connection(p_user, p_account_ref, p_refresh_token, p_history_id) returns uuid`, `gmail_get_refresh_token(p_user, p_connection) returns text`, `gmail_state(p_user, p_connection) returns table(cursor, last_success_at)`, `gmail_update(p_user, p_connection, p_cursor, p_status, p_watch_expires_at)`, `gmail_enqueue_for_account(p_account_ref text) returns uuid`, `gmail_enqueue_all(p_kind text) returns int`, `gmail_reauth_due() returns table(connection_id, user_id, reason)`(재인증 푸시 대상: `expires_at` 24시간 이내 또는 `reauth_required`).
  - `_shared/gmail.ts`: `exchangeCode`, `refreshAccessToken(refreshToken): Promise<string>`(invalid_grant → `ReauthRequired`), `watch`, `profile`, `listMessageIds`, `history`, `getMessage`, `plainText(msg)`, `header(msg, name)`, `gmailToItem(msg): GmailItem`(서버 규칙 필터 적용), `collectNewMessageIds(api: GmailApi, state: { cursor: string; lastSuccessAt: string }): Promise<{ ids: string[]; cursor: string; mode: "history" | "resync" }>`.
  - worker 잡 종류: `gmail-sync`(history → 404면 `messages.list(after:)` 재동기화), `gmail-fetch`(본문 → `gmailToItem`(규칙 필터) → `encrypt` → `insert_item`), `gmail-watch`(매일 watch 갱신). 모두 `lease_key = 'gmail:' + connection_id`.
  - `gmail-webhook`: Pub/Sub push 수신 → OIDC 검증 → `gmail_enqueue_for_account`.

- [ ] **Step 1: GCP 설정 (사용자, 1회)**

1. Google Cloud 프로젝트 생성 → Gmail API·Pub/Sub API 활성화.
2. OAuth 동의 화면: External, Testing, 테스트 사용자에 본인 Gmail 추가. 범위 `gmail.readonly`.
3. 클라이언트 2개: iOS(번들 `com.picpal.assistant.poc`)와 Web(시크릿 있음). Web 클라이언트 ID·시크릿을 Supabase secrets에.
4. Pub/Sub 토픽 `gmail-push`, `gmail-api-push@system.gserviceaccount.com`에 Publisher 권한. Push 구독 → 엔드포인트 `https://<ref>.supabase.co/functions/v1/gmail-webhook`, 인증: 서비스 계정 OIDC 토큰, audience 동일 URL. 토픽 전체 이름 `projects/<gcp-project>/topics/gmail-push`를 `GMAIL_PUBSUB_TOPIC`으로 `.env`와 secrets에.

- [ ] **Step 2: iOS에서 serverAuthCode 얻기**

`GoogleSignIn.swift`: GoogleSignIn-iOS SPM 추가(project.yml packages에 `https://github.com/google/GoogleSignIn-iOS` 8.x). 버튼 → `GIDSignIn.sharedInstance.signIn(withPresenting:hint:additionalScopes: ["https://www.googleapis.com/auth/gmail.readonly"])` → `result.serverAuthCode`를 `POST /functions/v1/gmail-connect` body `{ code, user_id }`로 전송(Supabase JWT 필요; PoC에서는 service role로 대체하고 `user_id`는 `.env`의 `POC_USER_ID`). `GIDConfiguration(clientID: iOS, serverClientID: Web)`.

- [ ] **Step 3: 마이그레이션 `0005_gmail.sql`**

```sql
create table connections (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users on delete cascade,
  provider text not null check (provider in ('gmail')),
  account_ref text not null,
  status text not null default 'active' check (status in ('active','reauth_required','disconnected')),
  expires_at timestamptz,                -- OAuth refresh token 만료. 테스트 모드 발급 후 7일, 앱 게시 후 null
  created_at timestamptz not null default now(),
  unique (provider, account_ref)
);
create table sync_states (
  connection_id uuid primary key references connections on delete cascade,
  user_id uuid not null references auth.users on delete cascade,
  cursor text not null,                   -- Gmail historyId
  last_success_at timestamptz not null default now(),
  watch_expires_at timestamptz            -- users.watch 응답 expiration (7일)
);
alter table connections enable row level security;
alter table sync_states enable row level security;
create policy connections_owner_read on connections for select using ((select auth.uid()) = user_id);
create policy sync_states_owner_read on sync_states for select using ((select auth.uid()) = user_id);

create or replace function gmail_save_connection(p_user uuid, p_account_ref text, p_refresh_token text, p_history_id text)
returns uuid language plpgsql security definer set search_path = '' as $$
declare v_id uuid; v_secret uuid;
begin
  insert into public.connections as c (user_id, provider, account_ref, status) values (p_user, 'gmail', p_account_ref, 'active')
  on conflict (provider, account_ref) do update set status = 'active' where c.user_id = excluded.user_id
  returning id into v_id;
  if v_id is null then raise exception 'account_ref linked to another user' using errcode = 'P0001'; end if;
  if p_refresh_token is not null then     -- 재동의가 아니면 Google이 refresh token을 다시 주지 않는다
    select id into v_secret from vault.secrets where name = 'gmail_rt:' || v_id;
    if v_secret is null then perform vault.create_secret(p_refresh_token, 'gmail_rt:' || v_id);
    else perform vault.update_secret(v_secret, p_refresh_token); end if;
    update public.connections set expires_at = now() + interval '7 days' where id = v_id;   -- OAuth 테스트 모드 수명
  end if;
  insert into public.sync_states (connection_id, user_id, cursor) values (v_id, p_user, p_history_id)
  on conflict (connection_id) do update set cursor = excluded.cursor, last_success_at = now();
  return v_id;
end $$;

create or replace function gmail_get_refresh_token(p_user uuid, p_connection uuid) returns text
language sql stable security definer set search_path = '' as $$
  select s.decrypted_secret from public.connections c join vault.decrypted_secrets s on s.name = 'gmail_rt:' || c.id
  where c.id = p_connection and c.user_id = p_user and c.status = 'active';
$$;

create or replace function gmail_state(p_user uuid, p_connection uuid)
returns table (cursor text, last_success_at timestamptz) language sql stable as $$
  select s.cursor, s.last_success_at from sync_states s where s.connection_id = p_connection and s.user_id = p_user;
$$;

-- null 인자는 바꾸지 않는다. cursor를 넘기면 동기화 성공으로 보고 last_success_at도 갱신한다
create or replace function gmail_update(p_user uuid, p_connection uuid, p_cursor text default null,
                                        p_status text default null, p_watch_expires_at timestamptz default null)
returns void language plpgsql as $$
begin
  update sync_states set cursor = coalesce(p_cursor, cursor),
                         last_success_at = case when p_cursor is not null then now() else last_success_at end,
                         watch_expires_at = coalesce(p_watch_expires_at, watch_expires_at)
  where connection_id = p_connection and user_id = p_user;
  if p_status is not null then
    update connections set status = p_status where id = p_connection and user_id = p_user;
  end if;
end $$;

-- 웹훅용: emailAddress로 연결을 찾아 gmail-sync 잡을 넣는다. 이미 대기 중인 sync가 있으면 넣지 않는다
create or replace function gmail_enqueue_for_account(p_account_ref text) returns uuid language sql as $$
  insert into jobs (kind, user_id, lease_key, payload)
  select 'gmail-sync', c.user_id, 'gmail:' || c.id, jsonb_build_object('connection_id', c.id)
  from connections c
  where c.provider = 'gmail' and c.account_ref = p_account_ref and c.status = 'active'
    and not exists (select 1 from jobs j where j.lease_key = 'gmail:' || c.id and j.kind = 'gmail-sync' and j.status = 'queued')
  returning id;
$$;

-- cron용: 활성 연결마다 잡 1개 (gmail-sync 6시간 대조, gmail-watch 매일 갱신)
create or replace function gmail_enqueue_all(p_kind text) returns int language plpgsql as $$
declare n int;
begin
  if p_kind not in ('gmail-sync', 'gmail-watch') then raise exception 'bad kind'; end if;
  insert into jobs (kind, user_id, lease_key, payload)
  select p_kind, c.user_id, 'gmail:' || c.id, jsonb_build_object('connection_id', c.id)
  from connections c
  where c.provider = 'gmail' and c.status = 'active'
    and not exists (select 1 from jobs j where j.lease_key = 'gmail:' || c.id and j.kind = p_kind and j.status = 'queued');
  get diagnostics n = row_count;
  return n;
end $$;

-- 재인증 푸시 대상(스펙 §7): refresh token 만료 24시간 이내, 또는 invalid_grant로 reauth_required가 된 연결
create or replace function gmail_reauth_due()
returns table (connection_id uuid, user_id uuid, reason text) language sql stable as $$
  select c.id, c.user_id, case when c.status = 'reauth_required' then 'invalid_grant' else 'expiring' end
  from connections c
  where c.provider = 'gmail'
    and (c.status = 'reauth_required' or (c.status = 'active' and c.expires_at < now() + interval '24 hours'));
$$;

revoke execute on function gmail_save_connection(uuid, text, text, text), gmail_get_refresh_token(uuid, uuid),
  gmail_state(uuid, uuid), gmail_update(uuid, uuid, text, text, timestamptz), gmail_enqueue_for_account(text),
  gmail_enqueue_all(text), gmail_reauth_due() from public, anon, authenticated;

select cron.schedule('gmail-sync-every-6h', '0 */6 * * *', $$ select gmail_enqueue_all('gmail-sync'); $$);
select cron.schedule('gmail-watch-daily', '17 3 * * *', $$ select gmail_enqueue_all('gmail-watch'); $$);
```
watch는 7일 뒤 만료되므로 매일 갱신해 하루 이상 실패해도 끊기지 않게 한다(스펙 §7 "매일 watch 갱신"). 재인증 푸시는 `gmail_reauth_due()`가 대상을 고른다. PoC에는 기기 토큰 테이블이 없어 대상 조회까지만 검증하고, APNs 발송(Task 9 `sendAPNs`)을 붙이는 notify 잡은 1단계에서 만든다.

- [ ] **Step 4: 실패하는 테스트 — 재동기화·본문 추출·재인증·cron 적재**

`tests/gmail.test.ts`:
```ts
import { assert, assertEquals, assertRejects } from "jsr:@std/assert";
import { stub } from "jsr:@std/testing/mock";
import { createClient } from "npm:@supabase/supabase-js@2";
import { collectNewMessageIds, type GmailApi, gmailToItem, plainText, ReauthRequired, refreshAccessToken } from "../functions/_shared/gmail.ts";

function fakeApi(o: { historyPages?: { ids: string[]; historyId: string }[]; notFound?: boolean; listPages?: string[][]; profileHistoryId?: string }) {
  const calls: string[] = [];
  const api: GmailApi = {
    history: async (_start, pageToken) => {
      calls.push("history:" + (pageToken ?? ""));
      if (o.notFound) return { notFound: true as const };
      const i = Number(pageToken ?? 0), p = o.historyPages![i];
      return { history: [{ messagesAdded: p.ids.map((id) => ({ message: { id } })) }], historyId: p.historyId,
               nextPageToken: i + 1 < o.historyPages!.length ? String(i + 1) : undefined };
    },
    listMessageIds: async (q, pageToken) => {
      calls.push("list:" + q);
      const i = Number(pageToken ?? 0);
      return { messages: o.listPages![i].map((id) => ({ id })), nextPageToken: i + 1 < o.listPages!.length ? String(i + 1) : undefined };
    },
    profile: async () => { calls.push("profile"); return { emailAddress: "poc@example.com", historyId: o.profileHistoryId ?? "0" }; },
  };
  return { api, calls };
}

Deno.test("history pages are merged and cursor moves to the last historyId", async () => {
  const { api } = fakeApi({ historyPages: [{ ids: ["a", "b"], historyId: "110" }, { ids: ["b", "c"], historyId: "120" }] });
  const r = await collectNewMessageIds(api, { cursor: "100", lastSuccessAt: "2026-09-20T00:00:00Z" });
  assertEquals(r, { ids: ["a", "b", "c"], cursor: "120", mode: "history" });
});

Deno.test("history 404 → profile first, then messages.list(after: last_success_at - 1 day) across pages", async () => {
  const { api, calls } = fakeApi({ notFound: true, listPages: [["m1", "m2"], ["m3"]], profileHistoryId: "999" });
  const r = await collectNewMessageIds(api, { cursor: "1", lastSuccessAt: "2026-09-20T00:00:00Z" });
  const after = Date.UTC(2026, 8, 20) / 1000 - 86400;
  assertEquals(calls, ["history:", "profile", `list:after:${after} -category:promotions`, `list:after:${after} -category:promotions`]);
  assertEquals(r, { ids: ["m1", "m2", "m3"], cursor: "999", mode: "resync" });
});

Deno.test("plainText prefers text/plain, skips attachments, falls back to stripped html", () => {
  const b64 = (s: string) => btoa(String.fromCharCode(...new TextEncoder().encode(s))).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
  const multipart = { payload: { mimeType: "multipart/mixed", parts: [
    { mimeType: "multipart/alternative", parts: [
      { mimeType: "text/plain", body: { data: b64("합성 주문 확인: 무선 이어폰") } },
      { mimeType: "text/html", body: { data: b64("<p>무시</p>") } } ] },
    { mimeType: "application/pdf", filename: "영수증.pdf", body: { attachmentId: "att1" } } ] } };
  assertEquals(plainText(multipart), "합성 주문 확인: 무선 이어폰");
  const htmlOnly = { payload: { mimeType: "text/html", body: { data: b64("<style>p{}</style><p>합성&nbsp;배송 <b>시작</b></p>") } } };
  assertEquals(plainText(htmlOnly), "합성 배송 시작");
});

Deno.test("refresh token invalid_grant → ReauthRequired", async () => {
  using _f = stub(globalThis, "fetch", () => Promise.resolve(new Response(JSON.stringify({ error: "invalid_grant" }), { status: 400 })));
  await assertRejects(() => refreshAccessToken("rt"), ReauthRequired);
});

Deno.test("gmailToItem applies server rules: promotion label, OTP, card masking in body and subject", () => {
  const b64 = (s: string) => btoa(String.fromCharCode(...new TextEncoder().encode(s))).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
  const msg = (body: string, subject: string, labelIds = ["INBOX"]) => ({ id: "m1", internalDate: "1790000000000", labelIds,
    payload: { mimeType: "text/plain", headers: [{ name: "Subject", value: subject }, { name: "From", value: "합성쇼핑 <shop@example.com>" }],
               body: { data: b64(body) } } });
  assertEquals(gmailToItem(msg("가을 세일", "세일", ["INBOX", "CATEGORY_PROMOTIONS"])), { kind: "discard", reason: "promotion" });
  assertEquals(gmailToItem(msg("인증번호 483920 을 입력하세요", "본인 확인")), { kind: "discard", reason: "otp" });
  assertEquals(gmailToItem(msg("카드 4532-0151-1283-0366 승인 32,000원", "결제 4532015112830366 완료")), {
    kind: "pass", sender: "합성쇼핑 <shop@example.com>", title: "결제 ************0366 완료",
    text: "카드 ****-****-****-0366 승인 32,000원", occurredAt: new Date(1790000000000).toISOString() });
});

Deno.test("daily watch cron enqueues one gmail-watch per active connection only", async () => {
  const sb = createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);
  const user = Deno.env.get("POC_USER_ID")!;
  await sb.from("jobs").delete().eq("kind", "gmail-watch");
  const { data: conns } = await sb.from("connections").insert([
    { user_id: user, provider: "gmail", account_ref: `a-${crypto.randomUUID()}@example.com`, status: "active" },
    { user_id: user, provider: "gmail", account_ref: `r-${crypto.randomUUID()}@example.com`, status: "reauth_required" },
  ]).select("id, status");
  const { count: active } = await sb.from("connections").select("*", { count: "exact", head: true }).eq("status", "active");
  const first = await sb.rpc("gmail_enqueue_all", { p_kind: "gmail-watch" });
  const second = await sb.rpc("gmail_enqueue_all", { p_kind: "gmail-watch" });   // 대기 중이면 중복 적재 안 함
  assertEquals(first.data, active);
  assertEquals(second.data, 0);
  const reauth = conns!.find((c) => c.status === "reauth_required")!;
  const { count } = await sb.from("jobs").select("*", { count: "exact", head: true }).eq("lease_key", "gmail:" + reauth.id);
  assertEquals(count, 0);
  assert(active! >= 1);
  await sb.from("jobs").delete().eq("kind", "gmail-watch");
  await sb.from("connections").delete().in("id", conns!.map((c) => c.id));
});

Deno.test("gmail_reauth_due lists expiring (<24h) and invalid_grant connections only", async () => {
  const sb = createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);
  const user = Deno.env.get("POC_USER_ID")!;
  const inHours = (h: number) => new Date(Date.now() + h * 3600_000).toISOString();
  const { data: conns } = await sb.from("connections").insert([
    { user_id: user, provider: "gmail", account_ref: `soon-${crypto.randomUUID()}@example.com`, status: "active", expires_at: inHours(3) },
    { user_id: user, provider: "gmail", account_ref: `later-${crypto.randomUUID()}@example.com`, status: "active", expires_at: inHours(72) },
    { user_id: user, provider: "gmail", account_ref: `bad-${crypto.randomUUID()}@example.com`, status: "reauth_required", expires_at: inHours(72) },
  ]).select("id, account_ref");
  const { data } = await sb.rpc("gmail_reauth_due");
  const due = new Map((data as { connection_id: string; reason: string }[]).map((r) => [r.connection_id, r.reason]));
  const [soon, later, bad] = conns!;
  assertEquals(due.get(soon.id), "expiring");
  assertEquals(due.has(later.id), false);
  assertEquals(due.get(bad.id), "invalid_grant");
  await sb.from("connections").delete().in("id", conns!.map((c) => c.id));
});
```
실행: `cd poc/server && deno test --allow-net --allow-env --allow-read --env-file=.env supabase/tests/gmail.test.ts` → `gmail.ts`가 없어 실패. 마지막 두 테스트(DB)는 `supabase db push` 후 통과한다(Task 8처럼 워커 cron을 잠시 멈춘다).

- [ ] **Step 5: `_shared/gmail.ts` 구현**

```ts
import { applyRules, maskSensitive } from "./rules.ts";

const G = "https://gmail.googleapis.com/gmail/v1/users/me";
export class ReauthRequired extends Error {}

export async function exchangeCode(code: string) {
  const r = await fetch("https://oauth2.googleapis.com/token", { method: "POST", headers: { "content-type": "application/x-www-form-urlencoded" },
    body: new URLSearchParams({ code, client_id: Deno.env.get("GOOGLE_CLIENT_ID")!, client_secret: Deno.env.get("GOOGLE_CLIENT_SECRET")!, grant_type: "authorization_code", redirect_uri: "" }) });
  if (!r.ok) throw new Error("token exchange " + r.status);
  return await r.json() as { access_token: string; refresh_token?: string; expires_in: number };
}
export async function refreshAccessToken(refreshToken: string): Promise<string> {
  const r = await fetch("https://oauth2.googleapis.com/token", { method: "POST", headers: { "content-type": "application/x-www-form-urlencoded" },
    body: new URLSearchParams({ refresh_token: refreshToken, client_id: Deno.env.get("GOOGLE_CLIENT_ID") ?? "", client_secret: Deno.env.get("GOOGLE_CLIENT_SECRET") ?? "", grant_type: "refresh_token" }) });
  if (!r.ok) {
    const j = await r.json().catch(() => ({})) as { error?: string };
    if (j.error === "invalid_grant") throw new ReauthRequired("invalid_grant");   // 테스트 모드 7일 만료·동의 철회
    throw new Error("token refresh " + r.status);
  }
  return (await r.json() as { access_token: string }).access_token;
}
export async function watch(accessToken: string, topic: string) {
  const r = await fetch(`${G}/watch`, { method: "POST",
    headers: { authorization: `Bearer ${accessToken}`, "content-type": "application/json" },
    body: JSON.stringify({ topicName: topic, labelFilterBehavior: "EXCLUDE", labelIds: ["CATEGORY_PROMOTIONS"] }) });
  if (!r.ok) throw new Error("watch " + r.status);
  return await r.json() as { historyId: string; expiration: string };   // expiration: epoch ms 문자열, 약 7일 뒤
}
export async function profile(accessToken: string) {
  const r = await fetch(`${G}/profile`, { headers: { authorization: `Bearer ${accessToken}` } });
  if (!r.ok) throw new Error("profile " + r.status);
  return await r.json() as { emailAddress: string; historyId: string };
}
export async function listMessageIds(accessToken: string, q: string, pageToken?: string) {
  const u = new URL(`${G}/messages`); u.searchParams.set("q", q); u.searchParams.set("maxResults", "100");
  if (pageToken) u.searchParams.set("pageToken", pageToken);
  const r = await fetch(u, { headers: { authorization: `Bearer ${accessToken}` } });
  if (!r.ok) throw new Error("messages.list " + r.status);
  return await r.json() as { messages?: { id: string }[]; nextPageToken?: string };
}
type HistoryPage = { history?: { messagesAdded?: { message: { id: string } }[] }[]; nextPageToken?: string; historyId: string };
export async function history(accessToken: string, startHistoryId: string, pageToken?: string): Promise<{ notFound: true } | HistoryPage> {
  const u = new URL(`${G}/history`); u.searchParams.set("startHistoryId", startHistoryId); u.searchParams.set("historyTypes", "messageAdded");
  if (pageToken) u.searchParams.set("pageToken", pageToken);
  const r = await fetch(u, { headers: { authorization: `Bearer ${accessToken}` } });
  if (r.status === 404) return { notFound: true };
  if (!r.ok) throw new Error("history " + r.status);
  return await r.json() as HistoryPage;
}
type Part = { mimeType?: string; filename?: string; headers?: { name: string; value: string }[]; body?: { data?: string; attachmentId?: string }; parts?: Part[] };
export type GmailMessage = { id: string; internalDate: string; labelIds?: string[]; payload?: Part };
export async function getMessage(accessToken: string, id: string) {
  const r = await fetch(`${G}/messages/${id}?format=full`, { headers: { authorization: `Bearer ${accessToken}` } });
  if (!r.ok) throw new Error("messages.get " + r.status);
  return await r.json() as GmailMessage;
}

function b64urlDecode(s: string) {
  return new TextDecoder().decode(Uint8Array.from(atob(s.replace(/-/g, "+").replace(/_/g, "/")), (c) => c.charCodeAt(0)));
}
function findBody(p: Part | undefined, mime: string): string | null {
  if (!p) return null;
  if (p.mimeType === mime && p.body?.data && !p.filename) return b64urlDecode(p.body.data);
  for (const c of p.parts ?? []) { const r = findBody(c, mime); if (r !== null) return r; }
  return null;
}
// 첨부(filename·attachmentId)는 읽지 않는다(스펙 §12 통제 2)
export function plainText(msg: { payload?: Part }): string {
  const text = findBody(msg.payload, "text/plain");
  if (text !== null) return text.trim();
  const html = findBody(msg.payload, "text/html") ?? "";
  return html.replace(/<(style|script)[\s\S]*?<\/\1>/gi, " ").replace(/<[^>]+>/g, " ")
    .replace(/&nbsp;/g, " ").replace(/\s+/g, " ").trim();
}
export function header(msg: { payload?: Part }, name: string): string | null {
  return msg.payload?.headers?.find((h) => h.name.toLowerCase() === name.toLowerCase())?.value ?? null;
}

// 서버 규칙 필터(§7): /ingest와 같은 applyRules. 프로모션 라벨·(광고)·OTP는 폐기, 카드·계좌는 본문·제목 모두 마스킹
export type GmailItem =
  | { kind: "discard"; reason: "otp" | "promotion" }
  | { kind: "pass"; sender: string | null; title: string | null; text: string; occurredAt: string };
export function gmailToItem(m: GmailMessage): GmailItem {
  const sender = header(m, "From"), subject = header(m, "Subject");
  const v = applyRules(plainText(m), { sender, title: subject, labels: m.labelIds });
  if (v.kind === "discard") return v;
  return { kind: "pass", sender, title: subject === null ? null : maskSensitive(subject), text: v.masked,
           occurredAt: new Date(Number(m.internalDate)).toISOString() };
}

export interface GmailApi {
  history(startHistoryId: string, pageToken?: string): Promise<{ notFound: true } | HistoryPage>;
  listMessageIds(q: string, pageToken?: string): Promise<{ messages?: { id: string }[]; nextPageToken?: string }>;
  profile(): Promise<{ emailAddress: string; historyId: string }>;
}
export function gmailApi(accessToken: string): GmailApi {
  return {
    history: (s, p) => history(accessToken, s, p),
    listMessageIds: (q, p) => listMessageIds(accessToken, q, p),
    profile: () => profile(accessToken),
  };
}

// 스펙 §7: history.list 전 페이지 처리 후 커서 갱신. 404면 last_success_at - 1일부터 messages.list(after:)로 재동기화
export async function collectNewMessageIds(api: GmailApi, state: { cursor: string; lastSuccessAt: string }) {
  const ids = new Set<string>();
  let pageToken: string | undefined, cursor = state.cursor;
  do {
    const h = await api.history(state.cursor, pageToken);
    if ("notFound" in h) return await resync(api, state.lastSuccessAt);
    for (const rec of h.history ?? []) for (const m of rec.messagesAdded ?? []) ids.add(m.message.id);
    cursor = h.historyId;
    pageToken = h.nextPageToken;
  } while (pageToken);
  return { ids: [...ids], cursor, mode: "history" as const };
}
async function resync(api: GmailApi, lastSuccessAt: string) {
  const { historyId } = await api.profile();   // 목록 조회 전에 새 커서를 잡아 그 사이 도착분을 다음 history가 받게 한다
  const after = Math.floor(new Date(lastSuccessAt).getTime() / 1000) - 86400;
  const q = `after:${after} -category:promotions`;
  const ids = new Set<string>();
  let pageToken: string | undefined;
  do {
    const p = await api.listMessageIds(q, pageToken);
    for (const m of p.messages ?? []) ids.add(m.id);
    pageToken = p.nextPageToken;
  } while (pageToken);
  return { ids: [...ids], cursor: historyId, mode: "resync" as const };
}
```
실행: `deno test ... supabase/tests/gmail.test.ts` → 7개 통과(순수 5개 + DB 2개).

- [ ] **Step 6: worker 잡 핸들러 `_shared/gmail-jobs.ts` — 본문은 `encrypt` 후 저장**

```ts
import type { SupabaseClient } from "npm:@supabase/supabase-js@2";
import { encrypt, toBytea } from "./crypto.ts";
import { collectNewMessageIds, getMessage, gmailApi, gmailToItem, ReauthRequired, refreshAccessToken, watch } from "./gmail.ts";
import type { Job } from "./job.ts";

function ids(job: Job) {
  if (!job.user_id) throw new Error("gmail job without user_id");
  return { user: job.user_id, conn: String(job.payload.connection_id) };
}
// refresh 실패(invalid_grant) → connections.status = reauth_required, 잡은 정상 종료해 재시도하지 않는다(스펙 §7)
async function accessToken(sb: SupabaseClient, user: string, conn: string): Promise<string | null> {
  const { data: rt, error } = await sb.rpc("gmail_get_refresh_token", { p_user: user, p_connection: conn });
  if (error) throw new Error("gmail_get_refresh_token " + error.code);
  if (!rt) return null;                   // 비활성 연결
  try { return await refreshAccessToken(rt as string); }
  catch (e) {
    if (!(e instanceof ReauthRequired)) throw e;
    await sb.rpc("gmail_update", { p_user: user, p_connection: conn, p_status: "reauth_required" });
    return null;
  }
}

export async function gmailSync(sb: SupabaseClient, job: Job): Promise<string> {
  const { user, conn } = ids(job);
  const token = await accessToken(sb, user, conn);
  if (!token) return "skipped";
  const { data, error } = await sb.rpc("gmail_state", { p_user: user, p_connection: conn });
  if (error) throw new Error("gmail_state " + error.code);
  const st = (data as { cursor: string; last_success_at: string }[])[0];
  if (!st) throw new Error("gmail_state not_found");
  const r = await collectNewMessageIds(gmailApi(token), { cursor: st.cursor, lastSuccessAt: st.last_success_at });
  for (let i = 0; i < r.ids.length; i += 50) {
    const e = await sb.rpc("enqueue_job", { p_user: user, p_kind: "gmail-fetch", p_lease_key: "gmail:" + conn,
      p_payload: { connection_id: conn, ids: r.ids.slice(i, i + 50) } });
    if (e.error) throw new Error("enqueue_job " + e.error.code);
  }
  // 잡을 모두 넣은 뒤에 커서를 옮긴다. 그 사이 실패하면 다음 sync가 같은 구간을 다시 받고 idempotency_key가 중복을 막는다
  const u = await sb.rpc("gmail_update", { p_user: user, p_connection: conn, p_cursor: r.cursor });
  if (u.error) throw new Error("gmail_update " + u.error.code);
  console.log(JSON.stringify({ connection_id: conn, mode: r.mode, new_ids: r.ids.length }));
  return r.mode;
}

export async function gmailFetch(sb: SupabaseClient, job: Job): Promise<string> {
  const { user, conn } = ids(job);
  const token = await accessToken(sb, user, conn);
  if (!token) return "skipped";
  for (const id of job.payload.ids as string[]) {
    const it = gmailToItem(await getMessage(token, id));          // /ingest와 같은 서버 규칙 필터
    if (it.kind === "discard") {
      console.log(JSON.stringify({ connection_id: conn, gmail_discard: it.reason }));   // 사유 코드만
    } else {
      const { error } = await sb.rpc("insert_item", {
        p_user: user, p_source: "GMAIL", p_idempotency_key: "gmail:" + id,
        p_sender: it.sender, p_title: it.title,                    // 제목은 카드·계좌 마스킹된 값
        p_content_enc: toBytea(await encrypt(user, it.text)),      // 평문 본문은 DB로 가지 않는다
        p_occurred_at: it.occurredAt,
      });
      if (error) throw new Error("insert_item " + error.code);
    }
    await new Promise((r) => setTimeout(r, 240));                  // 분당 250건 이하
  }
  return "fetched";
}

export async function gmailWatch(sb: SupabaseClient, job: Job): Promise<string> {
  const { user, conn } = ids(job);
  const token = await accessToken(sb, user, conn);
  if (!token) return "skipped";
  const w = await watch(token, Deno.env.get("GMAIL_PUBSUB_TOPIC")!);
  const u = await sb.rpc("gmail_update", { p_user: user, p_connection: conn,
    p_watch_expires_at: new Date(Number(w.expiration)).toISOString() });
  if (u.error) throw new Error("gmail_update " + u.error.code);
  return "watched";
}
```

`functions/worker/index.ts`의 `handlers`에 추가:
```ts
import { gmailFetch, gmailSync, gmailWatch } from "../_shared/gmail-jobs.ts";
// handlers 객체 안
  "gmail-sync": (j) => gmailSync(sb, j),
  "gmail-fetch": (j) => gmailFetch(sb, j),
  "gmail-watch": (j) => gmailWatch(sb, j),
```
같은 연결의 잡은 `lease_key`가 같아 한 번에 하나만 돈다(Task 8 `claim_jobs`). `gmail-fetch` 1건(50통 × 240ms ≈ 12초)은 임대 60초 안에 끝난다.

- [ ] **Step 7: gmail-connect (토큰 교환·watch·백필 적재)**

`functions/gmail-connect/index.ts`:
```ts
import { createClient } from "npm:@supabase/supabase-js@2";
import { exchangeCode, listMessageIds, profile, watch } from "../_shared/gmail.ts";
const sb = createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);
Deno.serve(async (req) => {
  const { code, user_id } = await req.json() as { code: string; user_id: string };   // 1단계에서는 Supabase JWT의 sub
  const t = await exchangeCode(code);
  const p = await profile(t.access_token);
  const w = await watch(t.access_token, Deno.env.get("GMAIL_PUBSUB_TOPIC")!);
  const { data: connId, error } = await sb.rpc("gmail_save_connection", { p_user: user_id, p_account_ref: p.emailAddress,
    p_refresh_token: t.refresh_token ?? null, p_history_id: w.historyId });
  if (error) return Response.json({ error: error.code }, { status: 500 });
  await sb.rpc("gmail_update", { p_user: user_id, p_connection: connId, p_watch_expires_at: new Date(Number(w.expiration)).toISOString() });
  let pageToken: string | undefined, pages = 0, count = 0;
  do {
    const l = await listMessageIds(t.access_token, "newer_than:90d -category:promotions", pageToken);
    const ids = (l.messages ?? []).map((m) => m.id);
    if (ids.length) await sb.rpc("enqueue_job", { p_user: user_id, p_kind: "gmail-fetch", p_lease_key: "gmail:" + connId,
      p_payload: { connection_id: connId, ids } });
    pageToken = l.nextPageToken; pages++; count += ids.length;
  } while (pageToken);
  return Response.json({ connection_id: connId, watch_expiration: w.expiration, backfill_pages: pages, backfill_messages: count });
});
```
`gmail-fetch`가 `format=full`로 본문을 받아 `gmailToItem`(규칙 필터: 프로모션·OTP 폐기, 카드·계좌 마스킹) → `encrypt` → `insert_item`(process 잡 동시 생성)으로 저장한다. 제목·발신자는 스펙 §8대로 평문 컬럼이다.

- [ ] **Step 8: 웹훅 OIDC 검증 테스트 → 구현**

`tests/gmail.test.ts`에 추가: `verifyPubSubToken(authHeader, audience, jwksOverride?)`가 (a) `aud` 불일치 → false, (b) `email`이 `PUBSUB_SA_EMAIL`과 다름 → false, (c) 정상 → true. Google 공개키는 `https://www.googleapis.com/oauth2/v3/certs`에서 JWKS로 받아 `jose`(`npm:jose@5`)로 검증. 테스트는 로컬에서 만든 RS256 키로 서명한 토큰과 JWKS를 주입해 돌린다.

`gmail-webhook/index.ts`: 검증 실패 401 → 본문 `message.data` base64 디코드 → `{emailAddress, historyId}` → `sb.rpc("gmail_enqueue_for_account", { p_account_ref: emailAddress })` → 잡이 생겼거나 이미 대기 중이면 200, 모르는 계정도 200(재전송 방지, 로그에는 `unknown_account`만). `verify_jwt = false`로 배포하므로 테이블 직접 조회 없이 저장 프로시저만 부른다(스펙 §7·§12 통제 4).

- [ ] **Step 9: 시나리오 (PoC-6)**

1. 앱에서 Google 로그인 → 연결 성공, `sync_states.watch_expires_at`이 약 7일 뒤인지 기록.
2. 백필: 3개월 메일 건수와 소요 시간, 429 발생 여부 기록. `items.content_enc`가 모두 채워지고 평문 본문 컬럼이 없는지 `select count(*) filter (where content_enc is null) from items where source='GMAIL'`로 확인(본문을 복호화해 보지 않는다).
3. 본인에게 메일 보내기 → 웹훅 수신까지 지연(초) 기록. 5회 평균.
4. `update sync_states set cursor = '1'`로 오래된 커서를 만들어 404 재현 → 워커 로그 `mode: "resync"`, 복구 후 누락 0인지(직전 24시간 Gmail 메시지 ID 집합과 `items.idempotency_key` 비교).
5. watch 갱신: `select gmail_enqueue_all('gmail-watch')`를 수동 실행 → `watch_expires_at`이 새 값(약 7일 뒤)으로 바뀌는지. 다음 날 cron 실행 후 한 번 더 확인.
6. 연결 6일 뒤 `select * from gmail_reauth_due()`에 `expiring`으로 나오는지(`connections.expires_at` 24시간 전).
7. 8일 후 refresh 실패 재현: 테스트 사용자 동의 7일 만료 → `connections.status = reauth_required`로 전환되고 `gmail_reauth_due()`에 `invalid_grant`로 나오며, 이후 gmail 잡이 `skipped`로 끝나는지. (달력에 표시하고 8일 뒤 확인)
8. 규칙 필터: 본인에게 합성 OTP 메일("인증번호 483920")과 카드번호가 든 메일을 보내 전자는 `items`에 없고(로그 `gmail_discard: otp`) 후자는 1행 생기는지 확인한다(본문은 복호화해 보지 않는다).

- [ ] **Step 10: 커밋** `git commit -m "poc(server): gmail connect, watch renewal, webhook with OIDC, history sync with resync, encrypted bodies"`

---

### Task 11: 한국어 하이브리드 검색 평가 (PoC-7)

> **임베딩 활성화 조건 (스펙 §12 통제 3·§16):** 약관 사유의 보류는 2026-09-26 OpenAI 데이터 정책 확인으로 해제됐다. 남은 조건은 **PoC-7 통과** 하나다. 이 태스크는 **합성 코퍼스(`eval/corpus.jsonl`)만** OpenAI로 임베딩하는 평가 태스크이고, Gmail 백필·공유 항목 등 실제 사용자 데이터에는 `embed()`를 호출하지 않으며 worker의 `process` 경로에도 임베딩을 연결하지 않는다. 그동안 검색은 `hybrid_search(p_embedding => null)` 키워드 경로만 쓴다.

**Files:**
- Create: `migrations/0004_hybrid_search.sql`, `functions/_shared/openai.ts`, `functions/_shared/embeddings.ts`, `eval/corpus.jsonl`, `eval/questions.jsonl`, `eval/run-search-eval.ts`, `tests/search.test.ts`

**Interfaces:**
- Consumes: Task 8의 `items`·`insert_item`·`encrypt`·`toBytea`, `.env`의 `POC_USER_ID`.
- Produces: `item_chunks(id, item_id, user_id, chunk_index, text, embedding vector(512) null, tsv tsvector)`, 함수 `hybrid_search(p_user uuid, p_query text, p_embedding vector(512), p_limit int, p_from timestamptz default null, p_to timestamptz default null) returns table(item_id uuid, chunk_id uuid, score float)` (RRF, k=60, pg_trgm 유사도 0.2 이상도 후보 포함. `p_embedding`이 null이면 키워드·trigram만으로 순위). `embed(texts: string[], inputType: "document"|"query"): Promise<number[][]>`(시그니처 유지, OpenAI는 `inputType`을 쓰지 않는다). `openai`(공유 클라이언트, Task 12가 재사용).

- [ ] **Step 1: 마이그레이션 `0004_hybrid_search.sql`**

```sql
create extension if not exists vector with schema extensions;
create extension if not exists pg_trgm with schema extensions;
create table item_chunks (
  id uuid primary key default gen_random_uuid(),
  item_id uuid not null references items(id) on delete cascade,
  user_id uuid not null references auth.users on delete cascade,
  chunk_index int not null,
  text text not null,
  embedding vector(512),                  -- text-embedding-3-small dimensions 512. PoC-7 통과 전 실제 데이터는 null
  tsv tsvector generated always as (to_tsvector('simple', text)) stored
);
create index on item_chunks using hnsw (embedding vector_cosine_ops);
create index on item_chunks using gin (tsv);
create index on item_chunks using gin (text gin_trgm_ops);
create index on item_chunks (user_id);
alter table item_chunks enable row level security;
create policy item_chunks_owner_read on item_chunks for select using ((select auth.uid()) = user_id);

create or replace function hybrid_search(p_user uuid, p_query text, p_embedding vector(512), p_limit int,
                                         p_from timestamptz default null, p_to timestamptz default null)
returns table(item_id uuid, chunk_id uuid, score float) language sql stable as $$
with base as (
  select c.id, c.item_id, c.text, c.embedding, c.tsv from item_chunks c join items i on i.id = c.item_id
  where c.user_id = p_user and i.user_id = p_user
    and (p_from is null or i.occurred_at >= p_from) and (p_to is null or i.occurred_at <= p_to)
), sem as (
  -- p_embedding이 null이면(임베딩 보류) 이 CTE는 비고 kw만으로 순위가 정해진다
  select id, row_number() over (order by embedding <=> p_embedding) rk
  from base where p_embedding is not null and embedding is not null
  order by embedding <=> p_embedding limit 40
), kw as (
  select id, row_number() over (order by greatest(ts_rank(tsv, plainto_tsquery('simple', p_query)), similarity(text, p_query)) desc) rk
  from base where tsv @@ plainto_tsquery('simple', p_query) or similarity(text, p_query) > 0.2 limit 40
), fused as (
  select coalesce(s.id, k.id) id, coalesce(1.0/(60+s.rk),0) + coalesce(1.0/(60+k.rk),0) score
  from sem s full outer join kw k on s.id = k.id
)
select b.item_id, f.id, f.score from fused f join base b on b.id = f.id order by f.score desc limit p_limit;
$$;
```

- [ ] **Step 2: 실패하는 테스트 — 임베딩 클라이언트, 임베딩 없는 검색**

`tests/search.test.ts`:
```ts
import { assert, assertEquals } from "jsr:@std/assert";
import { createClient } from "npm:@supabase/supabase-js@2";
import { encrypt, toBytea } from "../functions/_shared/crypto.ts";
import { embed } from "../functions/_shared/embeddings.ts";
const sb = createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);
const USER = Deno.env.get("POC_USER_ID")!;

Deno.test("embed returns one 512-dim vector (synthetic text only)", async () => {
  const v = await embed(["안녕하세요"], "document");
  assertEquals(v.length, 1);
  assertEquals(v[0].length, 512);
  const norm = Math.sqrt(v[0].reduce((a, x) => a + x * x, 0));
  assert(Math.abs(norm - 1) < 1e-3);   // dimensions 축소 후에도 길이 1 (cosine 전제)
});

Deno.test("hybrid_search works with p_embedding null (keyword/trigram only)", async () => {
  const text = "합성 문구: 무선 이어폰 주문 확인, 주문번호 T-4821, 결제 39,000원";
  const { data: itemId, error: e1 } = await sb.rpc("insert_item", { p_user: USER, p_source: "SHARE",
    p_idempotency_key: "search-test:" + crypto.randomUUID(), p_sender: null, p_title: "합성",
    p_content_enc: toBytea(await encrypt(USER, text)), p_occurred_at: new Date().toISOString(), p_enqueue: false });
  assertEquals(e1, null);
  await sb.from("item_chunks").insert({ item_id: itemId, user_id: USER, chunk_index: 0, text });   // embedding 없음
  const { data, error } = await sb.rpc("hybrid_search", { p_user: USER, p_query: "무선 이어폰 주문", p_embedding: null, p_limit: 5 });
  assertEquals(error, null);
  assert((data as { item_id: string; score: number }[]).some((r) => r.item_id === itemId && r.score > 0));
  await sb.from("items").delete().eq("id", itemId);
});
```
실행: `cd poc/server && supabase db push && deno test --allow-net --allow-env --allow-read --env-file=.env supabase/tests/search.test.ts` → `embeddings.ts`가 없어 실패.

- [ ] **Step 3: OpenAI 클라이언트·임베딩 구현**

`_shared/openai.ts` (Task 12도 이 파일을 쓴다):
```ts
import OpenAI from "npm:openai@7";
// Edge 벽시계 150초 안에 끝나도록 SDK 기본값(타임아웃 10분·재시도 2회)을 줄인다
export const openai = new OpenAI({ apiKey: Deno.env.get("OPENAI_API_KEY")!, timeout: 60_000, maxRetries: 1 });
```

`_shared/embeddings.ts`:
```ts
// 스펙 §12 통제 3·§16: 실제 사용자 데이터 임베딩은 PoC-7 통과 후. 그 전에는 합성 코퍼스(eval)에만 호출한다
import { openai } from "./openai.ts";
// OpenAI 임베딩은 문서/질의 구분이 없다. inputType은 호출부 시그니처 유지용이다
export async function embed(texts: string[], _inputType: "document" | "query"): Promise<number[][]> {
  const r = await openai.embeddings.create({ model: "text-embedding-3-small", input: texts, dimensions: 512, encoding_format: "float" });
  return [...r.data].sort((a, b) => a.index - b.index).map((d) => d.embedding);
}
```
요청 한도(스펙 §3): 입력당 8,192토큰, 요청당 합계 30만 토큰, 배열 2,048개. 512자 청크 × 배치 64는 한참 아래다.
실행: Step 2 명령 → 2개 통과.

- [ ] **Step 4: 합성 코퍼스·질문 작성**

`corpus.jsonl` 500건: `{"id":"c001","source":"GMAIL","occurred_at":"2026-07-03T09:12:00+09:00","text":"..."}`. 구성: 주문 확인 메일 150, 알림톡 150(주문·배송·예약), 병원·택배 문자 80, 사용자 발화 70(“지난주에 강남 카페 추천받았어” 등), 노이즈(뉴스레터·공지) 50. 전부 새로 지어낸 합성 문구로 쓰되 실제 문구 스타일을 흉내 내고, 동일 상품·동일 몰 변형을 일부러 넣는다. 실제 메일·문자 원문을 붙여 넣지 않는다(AGENTS.md §7).

`questions.jsonl` 50문항: `{"q":"에어팟 어디서 샀지","expect_ids":["c017"],"from":null,"to":null}`. 무근거 질문 10개는 `"expect_ids":[]`. 날짜 필터 질문 10개는 `from/to` 지정.

- [ ] **Step 5: 평가 스크립트**

`run-search-eval.ts`: 코퍼스를 적재한다 — 각 건을 `encrypt(POC_USER_ID, text)` → `insert_item(p_enqueue => false, p_idempotency_key => 'eval:' + id)`로 `items`에 넣고(process 잡을 만들지 않아 LLM 호출 없음), 512자 청크를 `item_chunks`에 넣으며 `embed(..., "document")` 배치 64로 `embedding`을 채운다. 이어서 각 질문을 두 방식으로 돌린다:
- **하이브리드**: `embed([q], "query")` → `hybrid_search` Top-5
- **키워드 전용**: `hybrid_search(p_embedding => null)` Top-5 — PoC-7 통과 전 실제로 쓰일 경로의 기준선

지표(두 방식 각각):
- 정답 포함률 = expect_ids 비어 있지 않은 질문 중 Top-5에 expect 하나라도 포함된 비율 (목표 ≥ 90%)
- 무근거 거절률 = expect 비어 있는 질문 중 Top-1 score < 임계(0.02)인 비율 (목표 ≥ 90%). 임계는 결과 분포를 보고 조정하고 값을 기록
- 날짜 필터 오판 = from/to 질문에서 범위 밖 결과가 Top-5에 들어온 건수

출력을 `docs/superpowers/poc/search-eval.md`에 두 방식을 나란히 둔 표로 저장. 끝나면 `eval:` 항목을 지우는 `--cleanup` 옵션을 둔다(`items` 삭제 시 청크는 cascade).

- [ ] **Step 6: 실행·기록·커밋**

`deno run --allow-net --allow-env --allow-read --allow-write --env-file=.env eval/run-search-eval.ts`. 미달 시 청크 256자, 모델 `text-embedding-3-large`(`dimensions: 512` 유지 — `vector(512)` 스키마 그대로), 키워드 가중치 변경을 각각 1회씩 시도하고 표에 나란히 기록. results.md PoC-7 행에 "통과 시 실제 데이터 임베딩 활성화 가능(약관 보류는 2026-09-26 해제, 스펙 §16)"을 함께 적는다. `git commit -m "poc(server): hybrid search (null-embedding path) + synthetic Korean eval corpus"`

---

### Task 12: 이미지·PDF 추출 (PoC-8 서버 부분)

**Files:**
- Create: `functions/vision-extract/index.ts`, `functions/_shared/extract.ts`, `eval/images/` (청첩장 5종, git 제외)

**Interfaces:**
- Consumes: Task 11의 `_shared/openai.ts`(`openai` 클라이언트). Task 11보다 먼저 시작하면 Task 11 Step 3의 `openai.ts`를 그대로 먼저 만든다.
- Produces: `extractEvent(input: { imageBase64?: string; mediaType?: string; pdfBase64?: string; ocrText?: string }): Promise<{ title: string|null; start: string|null; end: string|null; location: string|null; uncertain: string[] }>`

- [ ] **Step 1: 추출 함수 (Responses API + Structured Outputs)**

`_shared/extract.ts`:
```ts
import type OpenAI from "npm:openai@7";
import { openai } from "./openai.ts";
// strict 스키마: 모든 객체 additionalProperties false, 모든 필드 required, nullable은 타입 배열
const schema = { type: "object", additionalProperties: false, required: ["title", "start", "end", "location", "uncertain"],
  properties: { title: { type: ["string", "null"] }, start: { type: ["string", "null"], description: "ISO8601 +09:00" },
    end: { type: ["string", "null"] }, location: { type: ["string", "null"] },
    uncertain: { type: "array", items: { type: "string", enum: ["year", "ampm", "end", "tz", "date", "location"] } } } };
export type ExtractedEvent = { title: string | null; start: string | null; end: string | null; location: string | null; uncertain: string[] };
export async function extractEvent(input: { imageBase64?: string; mediaType?: "image/jpeg" | "image/png"; pdfBase64?: string; ocrText?: string }): Promise<ExtractedEvent> {
  const content: OpenAI.Responses.ResponseInputContent[] = [];
  if (input.imageBase64) content.push({ type: "input_image", detail: "high", image_url: `data:${input.mediaType ?? "image/jpeg"};base64,${input.imageBase64}` });
  if (input.pdfBase64) content.push({ type: "input_file", filename: "notice.pdf", file_data: `data:application/pdf;base64,${input.pdfBase64}` });
  if (input.ocrText) content.push({ type: "input_text", text: "OCR 텍스트:\n" + input.ocrText });
  content.push({ type: "input_text", text: "오늘은 2026-09-23이다. 이 청첩장/안내문에서 행사 일시와 장소를 추출하라. 연도가 없으면 uncertain에 year를 넣고 가장 가까운 미래 연도로 채워라. 오전/오후가 불명확하면 ampm을 넣어라." });
  const r = await openai.responses.create({
    model: "gpt-6-luna", store: false, reasoning: { effort: "none" }, max_output_tokens: 512,
    input: [{ role: "user", content }],
    text: { format: { type: "json_schema", name: "event", schema, strict: true } },
  });
  // 잘림·거절은 파싱하지 않고 실패로 돌린다. 오류 메시지에 본문을 넣지 않는다
  if (r.status !== "completed") throw new Error(`openai ${r.status} ${r.incomplete_details?.reason ?? ""}`.trim());
  if (r.output.some((o) => o.type === "message" && o.content.some((c) => c.type === "refusal"))) throw new Error("openai refusal");
  return JSON.parse(r.output_text) as ExtractedEvent;
}
```
스키마는 strict 규칙(스펙 §3)을 따른다. 이미지는 `input_image`(data URL), PDF는 `input_file`(`file_data` data URL). 확인: `deno check supabase/functions/_shared/extract.ts`.

- [ ] **Step 2: 함수와 시나리오**

`vision-extract/index.ts`: body `{ storagePath }` → Storage에서 다운로드 → base64 → `extractEvent` → JSON 반환. 청첩장 이미지 5종(연도 없는 것 2종, 모바일 청첩장 스크린샷 2종, PDF 안내문 1종)을 Storage `poc/` 버킷에 올리고 각각 호출. 정답표와 대조해 5/5 여부, `uncertain` 적중 여부, 건당 토큰(`r.usage.input_tokens`·`output_tokens`)·비용(`gpt-6-luna` $0.10/$0.50 per 1M)을 results.md PoC-8에 기록. 이미지 없이 `ocrText`만으로도 같은 5건을 돌려 OCR 대안의 정확도를 나란히 적는다.

- [ ] **Step 3: worker process 단계에 추출 연결 (복호화된 텍스트만 받는다)**

`functions/worker/index.ts`의 `process` 핸들러를 교체한다. 평문은 Task 8 `processItem`이 복호화해 넘긴 것만 쓰고, 로그에는 추출값을 남기지 않는다:
```ts
import { extractEvent } from "../_shared/extract.ts";
// handlers 객체 안
  process: (j) => processItem(sb, j, async (_userId, _itemId, text) => {
    const r = await extractEvent({ ocrText: text });
    console.log(JSON.stringify({ job_id: j.id, has_start: r.start !== null, uncertain: r.uncertain.length }));
    return "extracted";
  }),
```
확인: `scripts/seed-items.ts 3` → 3개 process 잡이 `done`, `checkpoint = 'extracted'`.

- [ ] **Step 4: 커밋** `git commit -m "poc(server): vision event extraction with structured output"`

---

### Task 13: 판정표 작성과 스펙 갱신

**Files:**
- Create: `docs/superpowers/poc/results.md`
- Modify: `docs/superpowers/specs/2026-09-22-assistant-design.md` §3, §5, §14, §16

- [x] **Step 1: results.md 템플릿 (Task 1 직후 만들어 두고 각 태스크가 채운다)**

현재 `results.md`의 형식이다. 측정값·채택 대안은 "근거 / 남은 실측" 열에 적는다.

```markdown
# PoC 판정표

상태: **통과** = 계획서 판정 기준을 실측으로 충족 · **부분** = 코드 경로만 확인(디버그 훅·시뮬레이터 대체) · **실패** · **미검증**. 다음 단계 진입 조건은 해당 PoC 가 **통과**인 것이다. 실기기 필요 항목은 실기기 세션 후 갱신한다.

| PoC | 검증 대상 | 태스크 | 상태 | 근거 / 남은 실측 | 갱신일 |
|---|---|---|---|---|---|
| PoC-1 | 단축어 Notification 트리거 → CaptureIntent 자동 실행 | 4 | | | |
| PoC-2 | 단축어 Message 트리거 → CaptureIntent 자동 실행 | 4 | | | |
| PoC-3 | Foundation Models 한국어 분류 200건 정확도·p95 | 5 | | | |
| PoC-4 | Edge Function → APNs HTTP/2 | 9 | | | |
| PoC-5 | 잠금화면 알림 액션 → 백그라운드 EventKit 멱등 쓰기 | 6 | | | |
| PoC-6 | Gmail watch → Pub/Sub → history 동기화 | 10 | | | |
| PoC-7 | 한국어 하이브리드 검색 Top-5 정확도 | 11 | | | |
| PoC-8 | 이미지·PDF → OCR/추출 → 일정 | 7, 12 | | | |
| PoC-9 | 앱 종료 후 background URLSession 업로드 완료 | 7 | | | |
| PoC-10 | jobs 큐 lease/재시도/dead 처리 | 8 | | | |

## 실기기 세션 대기 목록

## 참고
```

- [ ] **Step 2: 모든 행이 채워지면 스펙 갱신**

각 실패·대안 채택 항목에 대해 스펙 §3 표의 "설계 반영" 열과 §5 경로 표, §16 리스크를 수정한다. PoC-1이 "배너 필수"로 판정되면 §5 자동화 수준을 "배너 탭 1회(확정)"로 바꾸고 2단계 완료 기준에 "탭 1회로 수집" 문구를 넣는다. PoC-4 실패면 §4 그림의 notify를 "Cloudflare Worker 릴레이"로 바꾼다.

- [ ] **Step 3: 커밋** `git commit -m "docs: phase-0 PoC results and spec updates"`

---

## 실행 순서와 병렬화

| 순서 | 태스크 | 병렬 가능 | 실기기 |
|---|---|---|---|
| 1 | Task 1 골격 | — | 아니오 |
| 2 | Task 2·3 (필터·큐), Task 8 (Supabase·jobs) | 서로 병렬 | 아니오 |
| 3 | Task 4 (인텐트), Task 5 (FM), Task 9 (APNs 코드), Task 11 (검색) | 서로 병렬 | 4·5: 시뮬 먼저 |
| 4 | Task 6·7 (알림 액션·공유), Task 10 (Gmail), Task 12 (vision) | 서로 병렬 | 아니오 |
| 5 | **실기기 일괄 세션 1회**: PoC-1, PoC-2, PoC-3(시뮬 불가 시), PoC-4 토큰·발송, PoC-5 잠금 | — | 예 |
| 6 | Task 13 판정·스펙 갱신 | — | 아니오 |

Task 11은 Task 8의 `items`·`insert_item`·`crypto.ts`에 의존하므로 Task 8 완료 뒤 시작한다. Task 11은 합성 코퍼스만 임베딩한다. 실제 데이터 임베딩은 **PoC-7 통과 후**에만 활성화한다(약관 보류는 2026-09-26 해제, 스펙 §16). Task 12는 Task 11의 `_shared/openai.ts`를 쓴다.

실기기 세션은 5번 한 번으로 모은다. 그 전까지는 전부 시뮬레이터·서버에서 진행한다. 메모리 상 시뮬레이터 빌드와 deno 테스트를 동시에 돌리지 않는다.

## 사용자가 직접 해야 하는 것 (터미널·웹 콘솔)

1. Task 8: `supabase projects create`(org 선택), `supabase link`, `MASTER_KEY` 생성(`openssl rand -base64 32`)을 `.env`와 `supabase secrets set`에, `POC_USER_PASSWORD`를 정하고 PoC 사용자 생성 후 `POC_USER_ID`·`SUPABASE_ANON_KEY`를 `.env`에.
2. Task 9: APNs `.p8` 키 생성, `supabase secrets set`.
3. Task 10: GCP 프로젝트·OAuth 클라이언트 2개·Pub/Sub 토픽·푸시 구독 생성, 테스트 사용자 등록, `GMAIL_PUBSUB_TOPIC`을 `.env`와 secrets에.
4. Task 11·12: platform.openai.com에서 프로젝트 API 키 발급 → `.env`의 `OPENAI_API_KEY`와 `supabase secrets set OPENAI_API_KEY=...`. 조직 설정 → Data controls에서 학습 공유(옵트인)가 꺼져 있는지 확인한다. 결제 수단 등록·월 사용 한도(예: $10) 설정.
5. 실기기 세션: iPhone 연결, Xcode에서 Run, 단축어 자동화 3개(카톡 알림·인스타 알림·메시지) 수동 생성.
